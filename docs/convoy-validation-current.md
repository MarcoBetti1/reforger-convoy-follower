# Current validation checkpoint

**Current execution status — September 26, 2026:** the user subsequently resumed the full revised goal; saved-goal state is active (`updatedAt: 1790433506`). Earlier setup-only statements below record the completed documentation turn. Continue from current code and evidence under the latest user instructions, preserving all newer work. The previous turn made documentation progress; it did not run tests or change implementation.

Updated 2026-09-26 UTC. This is the concise status; the [edge-case matrix](convoy-edge-case-matrix.md) retains earlier runs. The mod is **not yet release ready**.

The complete [revised development brief](convoy-development-brief.md) supersedes the bounded overnight direction and is saved unchanged from the attachment. This documentation-only setup does not activate or resume the saved goal; further execution awaits the user's update and resumption. Preserve newer source and evidence rather than reverting to an older package.

**Current checkpoint:** preserve mod HEAD `3737f08`, its `72c911d` baseline, and every newer local source/evidence artifact. The independent harness remains at `419dba5`. The original-graph one-follower run is finalized as physical PASS / strict full-run FAIL; its two-follower run has closed normally with finalized physical and strict failure. Preserve the separately staged command repair and fixture draft without integrating them during setup. Rear-pacing, captured-Wait and trail-guide failures below remain unchanged. V6 input calibration passed only its narrow engine-action gate; ordinary desktop control is unresolved. None of these private experiments is promoted production behavior or a release pass.

## Latest preserved comparisons and exact next boundary

**Latest finalized result — original graph, one follower:** `paced-original-follow-1truck-v1` on `424A3383...` records **physical PASS / strict full-run FAIL**. Peak gap was **49.2195 m**; one continuous exact activity covered 320.568 m with 32 powered samples. Original pilot, truck and predecessor survived, followed by **180.364 seconds / 180 samples with zero measured drift** under the exact captured Wait. Six arrival reverse episodes and callbacks reporting failure after normal revocation remain. Natural closure and post-terminal observation are saved; **nine runtime and 64 shutdown errors are retained without waiver**. [Final independent report](../.cache/client/runs/paced-original-follow-1truck-v1/paced-independent-result.md), SHA-256 `12787755518DB3D52571A01D4CBB84522F183E0479164EAB64643B17FA94D54B`. This bounded comparison does not prove ordinary commands, smooth general driving or a release-ready backend.

**Latest finalized result — original graph, two followers:** `paced-original-follow-2truck-v1` on `223CA993...` records **physical FAIL / strict full-run FAIL**. Unit One/Two peaked at **46.0147 / 108.378 m**. Original trucks, pilots and immediate predecessors survived; both followers exceeded 100 m and three powered samples under exact activities. All three vehicles completed **180.312 seconds / 180 samples with zero measured drift** and exact owned follower Waits throughout. The saved [spacing diagnosis](../.cache/client/runs/paced-original-follow-2truck-v1/spacing-diagnosis.md) identifies native braking and an uphill near-stop while the same exact tail request remained open; the opaque planner's cause remains unproved. Native closure occurred after 30.0164 seconds, and **nine runtime / zero post-result / 64 shutdown errors remain unwaived**. [Final independent report](../.cache/client/runs/paced-original-follow-2truck-v1/paced-independent-result.md), SHA-256 `3413BB8AA3F04B870D5E4AC08D328073A3F570F2186D47E2DE91553C96EFC89E`, verifies the frozen source/deployment, terminal prefix and recording. Sampled visual review is not full playback. [Experiment record](convoy-original-follow-experiment.md).

**Preceding live comparison — diagnostic guide repeat FAIL:** `paced-trail-guide-1truck-v2` on `F134EAA1...` failed at **274.773 m** peak gap, with no formal hold. It genuinely joined the recorded route, but the same native activity braked and physically reversed before `OFF_ROUTE` (state 4). The guard uses distance to the retained segment; reversing behind its clamped start can trigger it without lateral road departure. No `ADVANCE_LIMIT` or rejected-query snapshot occurred, so the previous budget-rejection cause remains unresolved. Joined activity progress was 28.241 m net / 33.796 m maximum with five powered samples, below the unchanged 100 m gate. The later stall policy again removed membership. Natural client closure, recorder exit, all three deployment hashes, 26 frozen entries / 21 packed runtime resources, and the exact-prefix terminal snapshot were verified. Nine runtime and 64 shutdown errors remain. [Independent report](../.cache/client/runs/paced-trail-guide-1truck-v2/paced-independent-result.md), SHA-256 `2F75C92C814D205E3BA53ABF787C5255B66AF2E4F15170A6653A23B305B45614`; [experiment chronology and video](convoy-trail-guide-experiment.md). Two live captures do not establish full video review.

**Original-graph provenance:** the v2 package is `.cache/packed/convoy-original-follow-v2`, data SHA-256 `424A3383A06DB504106ED7CE6BE597B5BB3DA96156548366FF22BB15942830EB`. Its logs record successful WORKBENCH, PC, XBOX, PS4 and PS5 validation and packaging. The earlier v1 protected-priority compile failure and public-getter repair remain preserved. The two-follower v3 pack is `223CA993D3915D9805586149F63AC9352463F75563FFF61DBEA890B371A18B83`; it adds the distinct two-follower world without changing the one-follower graph/scripts. Existing results above are reconciled from saved artifacts, not new tests performed during this setup.

**Next boundary, after explicit resumption:** the two-follower independent analysis is now complete. Preserve that failed run rather than rebuilding or relaunching it because of an older pending note. Use its saved spacing diagnosis before choosing the next movement comparison. Independently review the cache-only command repair at `.cache/original-follow-command-candidate/` and the fixture draft at `.cache/original-follow-resume-fixture-candidate/`; neither is integrated or physically validated. Preserve the matching `5EA78318...` captured-Wait control, failed guide comparisons, and unchanged 60 m peak-gap / 180-second / 2 m paced-hold gates. Verify actual Hold/Resume, ownership and physical restart separately from route changes. No candidate is promoted or executed by this documentation update.

**Preceding live comparison — recorded trail guide FAIL:** the signature-only repair passed five-configuration validation and packaging as `CE3DEF31...`. `paced-trail-guide-1truck-v1` admitted the nonphysical guide and genuinely acquired the predecessor's first recorded segment. Spacing failed before that join; native folding/braking near x1405 preceded a later `ADVANCE_LIMIT` rejection. The exact rejected projection/budget was not logged. Final peak gap was 267.129 m, no formal hold occurred, and the later roster/StandDown path also invalidated original membership. Native closure, cleanup, terminal snapshot and recorder completion are preserved. See [the experiment record](convoy-trail-guide-experiment.md) for the frozen source/package, chronology and visual limits. This adapter remains private and unpromoted.

**Preserved diagnostic package:** `.cache/packed/convoy-trail-guide-v3`, data SHA-256 `F134EAA11615CEF065836F511F32532F01D60642B94D5BE0EFBE3594098BCDC3`. The saved logs in `.cache/workbench-runs/convoy-trail-guide-v3/` record successful validation for WORKBENCH, PC, XBOX, PS4 and PS5 and successful packaging. Its 26-file source manifest is `.cache/trail-guide-diagnostic-v3/source-manifest-v3.json`, SHA-256 `377E02D12B07B103C4C0003648A221F368BD92007B91893D25095387A0E7AE79`. That completed build was reused for the live repeat after explicit resumption; do not rebuild or rerun it solely because older setup notes called it pending.

**Preserved completed calibration:** v6 on frozen `5EA783183D6EDE9FCC5676FA16246E19062E4767DED909DEF4D3C847EA0ABC6D` passed independent review, all five configurations and packaging, then its predeclared engine-action gate. It recorded 453 writes/readbacks, 739 complete pairs, 405 powered intervals / 20.062 m, 20.0071 m signed travel and 2.49014 m uphill gain. Exactly one witnessed neutral-baseline sleep was classified without credit; raw interrupted count remains one, unexpected gaps/POSTs zero, and all numerical gates are unchanged. V3/v4/v5 remain failed. The [independent report](../.cache/client/runs/action-input-calibration-v6/action-input-independent-result.md) verifies original identities, source/deployment hashes and terminal snapshot, with scoped root live captures and natural closure. Nine runtime and 64 shutdown errors preserve strict full-run FAIL. Neutral release is not stopping proof; no ordinary keyboard/mouse, NPC-control, panel or delivery claim follows. [Calibration details](convoy-input-calibration.md) retain exact evidence.

**Finalized one-follower control:** `paced-entity-wait-1truck-v1` uses the same frozen `5EA78318...` pack and existing one-follower entity world, with captured Wait enabled and rear pacing disabled. Its lead MOVE radius is 5 m; private Follow distances remain 20 m moving / 10 m stopped, separately from the printed production completion default of 20 m. The unchanged 60 m peak-gap gate failed at **83.192 m**. Original identities, the exact predecessor, and powered progress survived; the same moving activity covered 322.330 m with 31 powered samples. All trucks then completed **180.18 seconds / 180 samples with zero drift**, with the follower's exact owned Wait selected throughout. Native uphill braking coincided with changed returned path geometry while the same Follow activity remained selected; causation is unproved. Normal client closure followed the post-terminal window. **Nine runtime and 64 shutdown errors remain**, so physical and strict full-run results are FAIL. The [final independent report](../.cache/client/runs/paced-entity-wait-1truck-v1/paced-independent-result.md), SHA-256 `B0162BB82BCFFF367B9C6E4BE4DB11251B80A3419CBA814ADA5A6BD268FED4E0`, retains full logs, the terminal snapshot, exact hashes, and a 379.733333-second recording. Visual coverage is two live captures, not full playback. This is existing evidence reconciled during setup, not a new live test.

The earlier comparisons below retain their original results; later execution updates above supersede historical preparation status.

**Callback-action v3: engine response observed, fixture FAIL.** The [independent result](../.cache/client/runs/action-input-calibration-v3/action-input-independent-result.md) records frozen package `78046F6B52CAF47154F88F36AFEA822E773C5CB585B3E11409DEFD960301B69A`, following successful five-configuration validation and packaging. On `Worlds/Tests/CF_ActionInput_Arland.ent`, 453 positive writes/readbacks accompanied **20.0152 m signed travel and 2.49131 m uphill gain**, with native powered controls and the original player pilot. The unchanged fixture nevertheless returned FAIL: 738 matched physics callbacks yielded zero same-pair powered-motion credit, and one unmatched callback remained. All 25 printed before/after entity-pose pairs were identical while successive pairs advanced. This supports repairing the observation boundary; it does not retroactively pass v3 or establish ordinary keyboard/mouse, steering, braking, UI restoration or precise stopping. There were 181 released-neutral callbacks. Natural client closure and recorder completion are recorded, with **nine runtime and 64 shutdown errors** retained. Full console SHA-256 `9BAAB79405B72D42CBA6D895A56A45C13F53A98844D8201B1840B179E97E5DB0`; terminal snapshot `026DD3B3A5F2E988D000159071B0C857BCE50384540B9CA8AA702E286089AE35`, verified prefix captured 87 ms after terminal. The private recording is 94.466667 seconds / 1,416 frames. Recorded visual inspection covers one live powered-leg capture, not full playback.

**Rear-aware pacing: physical and strict full-run FAIL.** The [final independent comparison](../.cache/client/runs/paced-entity-rear-2truck-v1/paced-independent-result.md) uses frozen package `BA45A5BA24CD7E5C473530CE04B18C7F02ED86BAD5814753622BEA159B604315`, built after all five configurations passed, on the existing two-follower entity course with unchanged 60 m spacing and 180-second / 2 m hold gates. Unit One / Two peak gaps worsened to **81.7383 / 101.084 m**. The original identities, immediate-predecessor chain, powered travel and activity retirement were retained. All three trucks then completed **180.626 seconds / 180 samples** with zero drift; both exact captured Waits remained selected throughout formal observation. Eligibility and native path/stop observations are saved separately in `rear-pacing-provisional.json`. The failed travel comparison and observed reversing are not erased by its stationary success; aligned arrival and repeatability remain unproved.

After explicit resumption, root consumed the existing client handle's natural exit 0 and recorder exit 0 at its 650-second duration limit, and verified no game, Workbench or recorder process remained. The finalized report retains **nine runtime and 64 shutdown errors** plus native close, cleanup and `Game destroyed`. Its SHA-256 is `96A810B9EB2A43B73E55EA642C7D9453CE421525BEF8CF1E78FBE69BADDA5117`. Full console hash: `381D07C51AD88FA8EC8B8E4FDA1736128032B2E80228F10E73B1F8C5D8B13408`; terminal snapshot: `4DAE01263F0BBFE5488E05DEC74FD3F1344F7D803E3C6802C8004DF15E8CA938`, verified prefix captured 43 ms after terminal. Video: 650 seconds / 9,749 frames / 519,583,760 bytes, without audio. Visual coverage remains three live captures, not full playback. The private rear-pacing enable was returned to zero for subsequent independent comparisons; its frozen failed package and raw artifacts remain unchanged.

**Newer callback-action results: v4 and v5 remain FAIL.** The [input-calibration record](convoy-input-calibration.md) supersedes the earlier v4 source-only label. Corrected v4 package `B5F2395E74AD67CDAD896257AB4AC5E2DC7AE2A79FCDE31C975CBB8A73E67ACC` and v5 observer package `3E241FA453B16D420C12BE120A0FB79EF7E3954601525B77A609F51FA0FD6790` each passed all five configurations and packaging, then produced 405 credited powered intervals / 20.062 m under the declared measurement. Signed travel was 20.0632 / 20.0648 m, with 2.49815 / 2.49839 m elevation gain. Each retained one unmatched callback and FAIL. V5 observed the original body's neutral-baseline sleep event with a pending PRE before the later drive-phase wake; this supports a narrowly prospective measurement correction, not a retrospective pass. Natural client/recorder closures are recorded, with nine runtime and 64 shutdown errors in each. V5 exercised the action fixture's single 100 ms early-close deferral; the separate paced branch is not thereby verified.

**Preserved candidate provenance:** v6 source SHA-256 `4C754B64F0AECC2BBCF3DB8C37D076B43F0D360C2AC670DF11E9544FD1E5B25C`, `.cache/action-input-v6-baseline-suspension-candidate.md`, and `.cache/action-input-calibration-manifest.json` identify the completed calibration above. The original writer/numerical gates stayed unchanged; failed older runs remain immutable. The separate `.cache/trail-guide-candidate/source-manifest.json` is frozen at SHA-256 `12CCBF88A7B181F048537EDBE29424B4DBF9734393E157BE0995BF5C1B5EE3F0`; its 14 new files and 12 dependencies passed read-only hash review. The existing `integration-v1.json` records those new files copied into the working tree before this setup reconciliation. Preserve them without compiling or launching during setup. Bounded source review identified no concrete new blocker, but generic-entity native Follow admission, genuine first-segment acquisition, turns, ownership/possession cleanup and physical gap/hold behavior remain unverified. The recorded-route budget is an experimental constraint, not general route correctness. The copied native behavior-tree redistribution boundary remains unresolved.

**Superseded preparation note:** the initial trail-guide validation and matched comparison have now completed as recorded above. Continue from the current next action; do not repeat the first comparison merely because the original setup queued it. All broader product, ordinary-input, terrain, cargo, multiplayer and distribution gates remain.

## Preceding result: captured Wait with two followers

This section reconciles existing artifacts only. The [independent report](../.cache/client/runs/paced-entity-wait-2truck-v1/paced-independent-result.md) records **physical FAIL / strict full-run FAIL** on frozen `D4E3874C...`, using the same two-follower course, 20/10 m moving/stopped gaps, radius 5, native cruise, requested lead speed 25 km/h, and unchanged 60 m / 180-second / 2 m gates as the `7C599AE1...` control. Unit One / Unit Two peak gaps were **52.1781 / 70.0129 m**. The stationary intervention occurs after those moving peaks; the smaller peak is not evidence that captured Wait improves travel pacing.

All original trucks, pilots and predecessor links survived, and both followers independently exceeded 100 m and three powered samples under a continuous exact native activity. The full **180.676-second** observation retained 180 samples per truck with **zero native and rounded-position drift**. Both private Follow activities were retired, and each follower's exact owned Wait was selected throughout all 180 formal samples. This is evidence for the isolated hold intervention. Four reverse-gear-correlated episodes on Unit One before capture remain; smooth arrival is not established. Wait priority 2100 deliberately overrides the observed native incoming-vehicle danger priority 1160. Safe dynamic threat response, restart, possession, explicit Hold/Resume and multiplayer remain separate gates.

The first candidate failed compilation with `Formula too complex`; a sequential short-circuit guard repair passed all five configurations and packaging in `.cache/workbench-runs/convoy-entity-wait-v2/`. The immutable pack is `.cache/packed/convoy-entity-wait-v2/`; the deployed three-file hashes and exact tested source are preserved in the run directory. Later source edits do not change this package's attribution. Natural closure completed 30.0156 seconds after the terminal marker, followed by cleanup, `Game destroyed` and client exit 0. **Nine runtime and 64 shutdown errors remain**, without waiver.

Full console SHA-256: `743F7D5CA0C6A97E5501ECF58DE23567BF8614C8EB38B059CACBB62EB4446E80`. Terminal snapshot: `9F76CBAFB116FF051CB4588B0BB0060249CC60CFEFAEB632FF19B8368B4C9DD7`, verified exact prefix captured 33 ms after the marker. Independent report: `94EE5B12EF373EB40EEAAEAA45510EAC5650A740A5176641FEAE791137ED4145`. The private video is 398.266667 seconds / 5,973 frames; recorded inspection covers three live captures, not extracted frames or full playback. Moving wheel evidence covers a mixed mapped dirt-road/asphalt course, not a wholly unpaved or off-network journey.

**Historical candidate preparation:** callback-action eligibility (`.cache/action-input-action-active-candidate.md`, probe hash `1DC76C0D77F29234E2D440C94A6CA433290DF05C33823DDF51F233C3DB495A8E`) and the rear-pacing candidate subsequently compiled and ran as recorded above. Their earlier source-only status is superseded. V4 and v5 subsequently compiled and ran as recorded above; v6 later completed its narrow calibration as recorded above; the first trail-guide physical comparison subsequently failed as recorded above. Preserve the original orchestration graph as inactive and resolve the copied-tree redistribution boundary before public integration.

**Ordinary input remains unresolved:** the saved desktop audit records fresh Notepad activation failures before delivery; no new tab, typed text or document modification occurred. An on-screen-keyboard overlay was observed but is not a proven cause. Positive OS-input calibration and engine-action injection are different checks. Keep this evidence in `.cache/ordinary-input-audit-2026-09-26.md` and the independent harness playbook; do not infer that a UI tool return or engine-action readback proves human controls work.

**Historical next-decision note:** the eligibility and rear-pacing comparisons proposed here have since run, with failures preserved above. Use the latest next-boundary section for resumption. Retain the original all-link 60 m and 180-second / 2 m gates; validate captured-Wait restart and ownership transfer before production promotion. The complete ordinary-control supply trip, broader routes, US/Soviet coverage, multiplayer, performance, asset provenance and demonstration remain on the roadmap. No build, launch, test, sync or goal transition is part of this setup.

## Preceding comparisons: two followers and isolated input

The following results and artifacts were already present when this documentation-only reconciliation began. They are preserved as existing evidence, not new tests performed or authorized by this setup. Their original acceptance thresholds remain unchanged.

**Two followers, physical and strict FAIL.** [Independent result](../.cache/client/runs/paced-entity-stop-2truck-v1/paced-independent-result.md), frozen `7C599AE1...`, world `Worlds/Tests/ConvoyFollower_Arland_OpenRoad_EntityFollow_2Trucks.ent`. Unit One / Unit Two peaks were **55.1046 / 76.1319 m**. Both original trucks, pilots and immediate predecessors survived; each follower independently exceeded 100 m and three powered samples under one uninterrupted exact native activity. Unit Two's initial launch gap had partly recovered before the later failure: at the first >60 m episode it was still following the same activity, requesting roughly 44 km/h with full throttle and no brake while actually traveling about 23 km/h, behind a predecessor doing about 41 km/h. This separates ongoing catch-up speed divergence from the earlier startup delay.

All three trucks completed **180.476 seconds / 180 samples each**. Lead and Unit One measured zero drift; Unit Two reached **7.57021 m native maximum drift** (7.560026 m from rounded positions). Both private entity orders were absent throughout observation. Unit Two had genuinely captured at 06:43:28.236, then reselected `SCR_AIPilotMoveFromIncomingVehicleBehavior` 0.302 seconds later. Its danger source was the actual predecessor, but the captured danger position was older than that predecessor's stationary pose. The independent danger continued after private order retirement. The coarse inherited arrival check began formal observation while Unit Two was already reversing; the unchanged full drift gate correctly failed and was not reset. Native behavior ownership during hold and travel pacing are separate next changes.

Natural exit occurred after 30.0157 seconds, followed by cleanup, `Game destroyed` and client exit 0. **Nine runtime and 62 shutdown errors remain**, without waiver. Full console SHA-256 `4AD7FBBF68BB45D5D7D14D17E9210092679BC3705A04248D28DA64D98316498A`; terminal snapshot `ACD09D3AA9A10BA5E563F229B5349C16625BE4FF0055EEDEDB9783F2FA2C4AB0`, verified exact prefix captured 4 ms after the marker. Private video: 411.733333 seconds / 6,175 frames; root inspected live captures and frames 60, 165 and 360, not full playback. Keep mixed mapped-road surface claims separate from wholly unpaved/off-network claims.

**Input calibration v1/v2: no input write occurred.** V1 on `7C599AE1...` instantiated the private character controller and reached the exact player pilot, but its combined readiness guard failed on the first callback. A reviewed probe-only change added individual before/after-native-control predicates and bounded observe-only readiness waiting; it passed all five configurations and packaging as `5BA9F239...`. V2 preserved identity and physical gates, with no context forcing. Every emitted pair over approximately 9.8 seconds had `CarThrust` active in both callback/global managers but `CarContext` inactive, with controls enabled, no menu/editor, and neutral values. It timed out after 601 prepare calls, 300 apply calls, 76 paired physics steps, zero unpaired steps and zero thrust writes. This rejects a mere first-callback race; it does not show native rejection of a delivered action. The context getter's installed timing contract needs investigation before changing that prerequisite. Engine injection would still not prove normal keys/clicks or human input restoration.

Both input runs closed naturally with client exit 0 and retained nine runtime / 64 shutdown errors each. V1 evidence is in `.cache/client/runs/action-input-calibration-v1/`; full log `019ABEEA9D64934A4D44D6008574EDD1F06812A930592F0721B9B7D0B2CD30D3`, terminal snapshot `79590C20538D1D27F1336B61B28FA073135C4D6F3AC42AA9D00D009BB00CE96E`. V2 evidence is in `.cache/client/runs/action-input-calibration-v2/`, terminal snapshot `53F5249047E9F3E29960D7D788EA4E10E3DB47EC26278C6ED7AF9EE1A74C2E76`. Their private videos are 71.266667 and 95.066667 seconds. Natural close did not exercise the newly compiled early-callback deferral branch.

**Historical prepared candidate:** the initial captured-Wait controller hash was `7947A5EBF4B6D7F94D748CA18C373833D49D92331569005A2C9BA15F1C2D224C`, with private prefab `B0BBC5FE9C2B4C4C0C1FC76412BD41874F502F861DC8389A70CFC858321B3CA8`. Preserve `.cache/entity-captured-wait-files.json` and `.cache/entity-captured-wait-experiment.md`. Compilation subsequently required the guard repair and produced the completed `D4E3874C...` comparison above; do not describe that experiment as still unrun. Its private priority intervention adds no second brake/cruise writer, and its successful hold does not establish safe production threats, spacing or restart.

**Historical next-step note:** the proposed captured-Wait comparison has now completed as recorded above; preserve its failure and hold evidence rather than rerunning it merely because this older note existed. Use the current pending-source and next-decision paragraphs for future resumption. The original orchestration graph remains inactive, with its scoped ownership review in `.cache/entity-follow-guarded-orchestration-review.md`; copied-tree redistribution and production integration remain unresolved. The three-follower extension remains compiled-only.

## Preserved one-follower stopped-entity result

This setup reads existing evidence; it does not rerun the experiment. The completed [independent report](../.cache/client/runs/paced-entity-stop-1truck-v1/paced-independent-result.md) records **physical fixture PASS / strict full-run FAIL** on frozen `E01A1EC3...`, after five-configuration validation and packaging. Its unchanged one-follower course retains the original truck, pilot and predecessor identities, a requested native lead speed of 25 km/h, 20/10 m moving/stopped gaps, and the 60 m peak-gap / 180-second hold / 2 m drift gates. Actual lead speed reached 32.2175 km/h; the request is not a measured speed bound.

The moving 20 m entity-follow activity was replaced once with a 10 m activity targeting the same stopped predecessor, without the previous ordinary-MOVE arrival fallback. Physical capture retired that activity after measured slow, stable arrival and retained the existing brake/cruise owner. Peak link separation was **52.0785 m**. Both original trucks completed **180.313 seconds / 180 formal samples each**, with zero native-reported and independently measured rounded-position drift. Powered travel and activity retirement passed. Four follower reverse episodes were independently corroborated, three after the stopped-target replacement; this is not a smooth-arrival or repeatability result. Wheel contacts establish a mixed mapped-road course, not an entirely unpaved or off-network journey.

**Nine exact vanilla-matched runtime errors remain, without waiver.** The scheduled native-close callback fired after **29999.8 ms**, refused the 30-second minimum, and did not retry. After supported desktop activation failed, the exact owned process was forcibly stopped. Normal cleanup and shutdown are unobserved; the lack of shutdown entries is not a clean lifecycle. The report preserves the forced-stop metadata correction. Full console SHA-256 is `0FBD70552F9C770872AF7B7E424EB13423B73B65BAB8706B6D8B01EB1995C3F1`; terminal snapshot SHA-256 is `E80A113AE6172FEA813F61124523E67275E53786EFEDD64EE4104AD688CB61DE`, verified as an exact prefix captured 3 ms after the marker. The private video is 503 seconds / 7,544 frames; recorded inspection covers live captures and five sampled frames, not full playback.

Explicit Hold/restart, multi-follower behavior, route fidelity, normal human input, multiplayer and production integration remain separate gates. The private copied behavior tree's redistribution basis is unresolved. Preserve this result alongside the earlier failed arrival comparison below.

## Preceding isolated entity-follow comparison

See the [entity-follow comparison note](convoy-entity-follow-comparison.md) for the isolated design, moving/arrival split and remaining gates.

The preserved isolated prototype passed all five script configurations and packaging as `convoy-entity-follow-v2`, frozen data SHA-256 **`21658B5401A8FF709BE5AC031FA78F78F35183E910782CAABE051A1074DABBCE`**. The first validation failed on the reserved local identifier `owned`; v2 corrected that identifier. The prototype files and design are preserved in `.cache/entity-follow-prototype-files.json` and `.cache/entity-follow-prototype-plan.md`. Its private Bohemia-derived behavior tree still requires redistribution review before a public checkpoint includes it. Ordinary production MOVE remains unchanged; the prototype introduces no additional steering, brake or cruise writer.

The completed `.cache/client/runs/paced-entity-follow-1truck-v1/paced-independent-result.md` records **physical and strict FAIL**, with a useful moving-leg improvement. The world is `Worlds/Tests/ConvoyFollower_Arland_OpenRoad_EntityFollow_1Truck.ent`, using the same one-follower geometry, requested lead 25 km/h, native cruise, moving/stopped gaps 20/10 m and ordinary MOVE radius 5 as the control below. The private entity waypoint requests 20 m distance. Peak separation **50.7126 m** passes the unchanged 60 m gate, compared with the ordinary-MOVE control's 140.223 m. One exact private activity remained selected for 52 consecutive one-second observations, independently covering **326.324528 m with 30 powered samples**. Original truck, pilot and predecessor identities were retained. The initial uphill reverse was absent; at the old reversal site the returned car path had five points and the truck continued forward. These results do not prove driven-trail fidelity or general repeatability.

Arrival still failed. At 05:54:41.971 the private order was replaced by an ordinary fixed MOVE. Junction reversing followed under `SCR_AIMoveActivity` / `SCR_AIMoveInFormationBehavior`, with no incoming-danger transition. The inherited route gate accepted net displacement at five-second samples and opened observation at 05:55:00.487 while the follower was still reversing at 1.74216 km/h. The stronger unchanged observation failed 2.386 seconds later. Across **180.379 seconds / 180 samples per truck**, lead drift was zero and follower maximum native drift was **5.28124 m** (5.272277 m independently from rounded vectors). Every observation sample had zero active private activities and no private waypoint; this is an ordinary-MOVE fallback/arrival failure, not a surviving Follow order. Later stable positions do not erase the original drift. Supplemental samples remain 5.262414 m from the observation origin, rather than showing continuing motion of that size.

The native close completed normally: request at 05:58:30.887 after 30.0163 seconds, `PACED_CLEANUP` at 05:58:31.274, and `Game destroyed` at 05:58:31.468. Root confirmed natural client exit 0 and no remaining game/Workbench/recorder process. **Nine exact vanilla-matched runtime errors and 64 shutdown errors remain**, with no waiver. The terminal snapshot is a verified full-log prefix captured 96 ms after the marker. Full log SHA-256 is `DE789E59312D295AE3D112CE9DC548AD5AE1D38FA6F17667D7B1558B6044B24E`; the report retains exact deployed/source/snapshot hashes and error mappings.

The private recording contains 4,991 frames / 332.8 seconds. Root inspected frames at 60, 110, 140 and 280 seconds plus live captures: road travel, junction approach and stationary arrival, with a thin OSK border at the top. These are sampled visual checks, not full playback or independent reverse-velocity proof. Wheel contacts establish mixed mapped dirt-road/asphalt coverage for both trucks, not wholly unpaved or off-network driving. The candidate is **not promoted**. Preserve its moving-leg result and diagnose ordinary stopped approach and truthful settling separately; do not reset or weaken the failed observation. Human input, explicit Hold/resume, multiple followers, multiplayer and full supply workflow remain separate gates.

## Preserved candidate construction and remaining verification

Saved logs in `.cache/workbench-runs/convoy-entity-stop-v3/` show successful WORKBENCH, PC, XBOX, PS4 and PS5 validation and packaging. The frozen data hash is **`7C599AE16FBEBFDA519A3CB1BF41B4BD1B2280F62098685816317EB379AD6F5B`**; the preparation manifest is `.cache/client/runs/action-input-calibration-v1/deployment-hashes.json`. Its subsequent two-follower and input results are recorded above. Keep the earlier v2 compile failure and its corrected identifier in the record.

- **Stopped approach:** retain the completed `E01A...` result above; do not call it an unrun draft or silently transfer its outcome to the newer pack. The private controller remains separate from normal driver prefabs.
- **Two-/three-follower extensions:** the two-follower run above preserves identities and continuous powered activities but fails spacing/hold; the three-follower extension remains compiled-only. Each must establish every original driver, truck and immediate predecessor independently.
- **ActionInput calibration:** the private character-controller fixture investigates supported action values before native control preparation. The pre-/post-physics forward-gear requirement of at least 2 is applied and compiled; live delivery and powered motion remain unverified. Engine-action injection cannot establish ordinary key/click delivery, panel usability or restored human driving controls.
- **Native exit correction:** the compiled fixtures allow one 100 ms deferral for a callback at most 100 ms early, then recheck all guards and the full 30-second minimum. The subsequent ordinary native-close paths succeeded; the early-deferral branch remains unexercised.
- **Original orchestration graph:** a separate original graph is present but inactive. Compilation does not prove its runtime ownership, native cancellation behavior, equivalence or redistribution suitability; the private copied baseline remains unpromoted.

After the user updates and resumes the goal, preserve the 60 m peak-gap gate, 180-second observation and 2 m drift limit in subsequent pacing and hold/restart comparisons. The captured-Wait comparison has already completed; use the next decisions at the top of this record. Do not redo the failed physics-write matrix or first-frame wait hypothesis without new evidence. The complete brief, broader road/input/supply/multiplayer gates and production-integration requirements remain in scope. No implementation, build, launch, test, repository sync or goal transition is authorized by this setup turn.

## Latest one-follower driving and native-close result

The completed `.cache/client/runs/paced-native-close-1truck-v1/paced-independent-result.md` records **physical and strict FAIL** on `CF7B2B77...`. Validation and packaging passed in `.cache/workbench-runs/convoy-paced-native-close-v1/`. The opt-in world is `Worlds/Tests/ConvoyFollower_Arland_OpenRoad_Paced_AutoExit_1Truck.ent`: **one follower plus the native lead**, with stable-waypoint retention disabled, native cruise enabled, moving/stopped gaps 20/10 m, radius 5 and the same requested lead cap of 25 km/h. Its new fixture option changes terminal closure only; it does not change follower movement or acceptance gates.

Peak separation **140.223 m** fails the unchanged 60 m limit. The initial uphill reverse recurred with no successor truck and no observed incoming-danger transition. The first 18 of 19 returned car-path points exactly match the earlier two-follower baseline's backward connector at logged precision; only the final endpoint differs and matches the updated goal. Original truck/pilot/predecessor identities and powered travel passed; all bound-roster ticks 6–283 contain both units. This is an additional one-follower baseline, not an improvement attributable to changing follower count or the shutdown option.

The formal hold passed **180.363 seconds / 180 samples per truck**. Lead drift was 0 m; follower maximum native-reported drift was **0.0116703 m**, with **0.014142 m** independently measured from rounded logged vectors. Both are below the unchanged 2 m gate; the follower drift is not zero. Later five-second samples span another 25.017 seconds with the same small rounded displacement. Moving wheel contacts again establish a mixed mapped-road course with dirt road and later asphalt, not an entirely unpaved or off-network result.

The separate **native-close lifecycle succeeded in this opt-in offline trial**. `PACED_RESULT: FAIL` and exit scheduling occurred at 05:38:43.694; one `PACED_EXIT_REQUESTED` followed at 05:39:13.711 after 30.0164 seconds. `WORLD_CLEANUP`, `PACED_CLEANUP` and `Game destroyed` followed at 14.051, 14.087 and 14.286. Root confirmed natural client exit 0 and no remaining Reforger process; no desktop close or 540-second force fallback was used. **Nine exact vanilla-matched runtime errors and 64 shutdown errors remain**, so normal closure is not an error-free lifecycle or a driving pass.

The terminal snapshot is an exact full-log prefix, with the read starting 55 ms and capture completing 56 ms after the marker. Full log SHA-256 is `F1AB4DBAB9175942984789E2977116B0257C827F9BD6C54A7C9F0301AC62DE6F`; the complete report retains snapshot/deployment hashes and exact error mappings. The private recording contains 5,471 frames / 364.8 seconds. Root inspected frames at 60, 100 and 320 seconds: visible large-gap travel, junction approach and stationary arrival, with only a thin OSK border at the top. These samples and live stills do not constitute full playback or independent reverse-velocity proof. Native exit does not establish ordinary key/button delivery, multiplayer behavior or Workbench exit behavior.

## Latest stable-waypoint comparison and preceding baseline

The completed `.cache/client/runs/paced-stable-waypoint-2truck-v1/paced-independent-result.md` records **physical and strict FAIL** on `E765E6B4...`. WORKBENCH, PC, XBOX, PS4 and PS5 validation and packaging passed in `.cache/workbench-runs/convoy-stable-waypoint-v1/`. The same road course, native lead request of 25 km/h, moving/stopped gaps of 20/10 m, completion radius 5 and original follower identities were retained; the profile enabled only the default-off `m_bStableFollowWaypoints` comparison setting.

Peak predecessor gaps worsened to **169.224 m / 131.836 m**, both above the unchanged 60 m gate. The original uphill reverse near x1405 did not recur in the observed path window, but fixed short goals repeatedly produced native completion, Idle/braking and restart. New MOVEs were issued 0.215–0.999 seconds after the first observed waypoint absence; emitted forward-powered samples still followed creation by approximately 2.84–5.14 seconds. The first spacing failure occurred before the old reversal site. Original truck/pilot/predecessor identities and powered travel passed. All three trucks completed **180.363 seconds / 180 samples with zero drift**, followed by 210.133 seconds of supplemental stationary samples. Keeping a destination fixed removed one observed reversal in this run while making both spacing results worse; it is not a driving fix.

The first terminal snapshot is an exact full-log prefix, captured 17 ms after the marker. **Nine exact vanilla-matched runtime errors remain**; none are waived. Desktop activation and both Alt+F4 attempts failed before delivery. The client ended through its existing **540-second forced limit**, without cleanup or `Game destroyed`; normal lifecycle and shutdown errors are unobserved, not clean. The private recording contains 9,183 frames / 612.266667 seconds. Root inspected frames at 60, 90 and 330 seconds: the main viewport is visible at departure, large-gap travel and arrival, with only a thin OSK border at the top. These three samples are not full playback or independent proof of reverse absence.

The saved `.cache/native-waypoint-queue-analysis.md` examined native completion and cancellation and motivated the now-completed private entity-follow comparison above. That run improved the moving leg but failed arrival observation; queued-MOVE continuation remains a separate, unvalidated hypothesis. Preserve unchanged physical gates, exact target/waypoint identities and possession cleanup; defer simultaneous pacing changes. The [comparison note](convoy-stable-waypoint-comparison.md) records the completed result and proposed next comparison. Ordinary input remains a separate gate in [the input investigation](convoy-input-investigation.md).

The completed `.cache/client/runs/paced-native-path-2truck-v2/paced-independent-result.md` records **physical and strict FAIL** on observer pack `8C58BE9E...`. Unit One and Unit Two peak predecessor gaps are **123.379 m / 88.5554 m**, exceeding the unchanged 60 m gate. Powered travel and original truck/pilot/predecessor identities passed. All three trucks completed **180.814 seconds / 180 samples with zero measured drift**, plus 50.017 seconds of supplemental stationary samples. Normal cleanup and `Game destroyed` are observed, retaining **nine vanilla-matched runtime errors and 64 shutdown errors**; classification waives none. The terminal snapshot is an exact full-log prefix captured about 101 ms after the marker. The full 360.066-second private recording is preserved; its entirety has not been independently replayed.

That baseline's native path telemetry shows an updated endpoint accompanied by a backward local connector before Unit One brakes and reverses. It motivated the now-completed waypoint-retention comparison above without proving the opaque native planner's cause. The candidate remains off by default and applies only during ordinary following with a moving predecessor; slow/stationary predecessor updates remain possible. Preserve both runs. Do not redo completed historical radius or input matrices without a new hypothesis.

Storage recovery and generic launch preflight are already completed work: 279 recordings (30,327,459,293 bytes) were hash/length verified on D:, preserving their old paths through a junction. The preflight refuses launches below 2 GiB available on the actual output volume; it does not reserve capacity. These are preserved results. The current setup is documentation-only and does not authorize implementation, builds, live tests, launches, repository sync or a saved-goal transition.

The [pause checkpoint](convoy-pause-2026-09-26.md) remains historical evidence. Newer local source, reports, and the native-hold experiment already exist beyond that checkpoint and are preserved. The full brief is saved byte-for-byte from the attachment (SHA-256 `A5231C6A57F11876D27A09355300B9555216E64D3F6D1EA4931BB34447583D12`).

## Preserved compiled combined candidate and later evidence

Preserve the following package and run artifacts. The brief remains unchanged; further execution awaits the user's explicit update and resumption.

The combined candidate now passed installed script validation in **WORKBENCH, PC, XBOX, PS4 and PS5**, followed by packaging. Logs are in `.cache/workbench-runs/convoy-departure-panel-v1/`; frozen `.cache/packed/convoy-departure-panel-v1/data.pak` SHA-256 is **`C657779ABA984D49894FBBD9623DBFB6AB993DE898B22347A65C9969CA627ACD`**. The deployed project and resource-database hashes are `5ACD6E066FEADC63513C6ED1476B4564AEDD8C91ADA74124EA97FE09D177C6CF` and `7A224A5BA9334B964E937AF686115B0D86864C486EC3B7EFA94467D9B6136C2F`, recorded with the package in the new run's deployment manifest.

This package includes the initial-departure controller/`CF_InitialDepartureWait` candidate, guarded panel-button resolution and bounded down/up/click traces, read-only incoming-vehicle danger telemetry, and the one-follower paced-world variant. These are **compiled and packaged**, with individual physical outcomes still requiring their relevant fixture. The initial-departure prototype's reverse/lateral-departure limitation remains explicit. The panel resolver is not an input fix until actual callback, command and menu evidence establishes it. See [focused paced and panel courses](convoy-feature-courses.md).

The completed same-course `.cache/client/runs/paced-road-2truck-departure-v1/paced-independent-result.md` records **physical and strict FAIL**. Per-link peaks were **103.742 m / 121.072 m**, still above the unchanged 60 m gate. The route and powered-travel gates passed; original truck/pilot/predecessor identities remained intact. All three trucks then completed **180.513 seconds / 180 samples with zero measured drift**, with the native lead Wait selected. Normal cleanup and Game destroyed are observed, retaining **nine runtime and 64 shutdown errors**. The first terminal snapshot was an exact log prefix captured about 87 ms after the marker. The private 333.26-second recording has not been independently reviewed here.

The candidate reduced the worst link from the earlier run's 230.336 m, but Unit One's peak increased from 66.4234 m to 103.742 m; this is not a spacing solution. Ordered departure-Wait release is observed. The saved `departure-comparison.md` establishes Unit One reversing under MoveFormation before a selected danger reaction, while Unit Two later reacted to an exact incoming-vehicle danger sourced from Unit One. These are separate causal observations; no global navigation fix follows. Moving natural wheel samples and later asphalt again establish a mixed-surface mapped-road course. The separate completed `panel-input-events-v3` run has no input pass, as detailed below. The one- and three-follower paced variants remain unverified in this combined package.

## Latest saved arrival-hold evidence and unfinished status work

Read-only reconciliation found the completed `.cache/client/runs/arrival-zero-radius5-v1/result.md` and `strict-full-report.json`. The frozen package is `.cache/packed/convoy-arrival-zero-v1/data.pak`, SHA-256 `30D094F57232348A4E891AA35FA46FF06D480A4CC20C7396CBCB605AB98C5225`. The one-follower taxiway comparison uses radius 5, moving gap 20, stopped gap 10 and the production cruise writer. The production change requests zero cruise while ordinary arrival is latched; the follower fixture only observes movement and does not provide its brake/navigation behavior.

- Physical travel, first hold and powered restart passed. Peak gap **47.3505 m** remains below the unchanged 60 m gate; final link gap is 8.24712 m.
- The stronger post-terminal observation records 180 ordered samples per truck: follower drift **0 m**, lead maximum drift approximately **0.000273 m**. The selected held follower behavior was native `SCR_AIIdleBehavior_Driver` with no waypoint. These samples establish a bounded observation, not indefinite hold or before-physics ownership.
- Strict acceptance still fails on **nine runtime and 64 shutdown errors**. Saved vanilla matches classify the runtime signatures without waiving them. Normal cleanup and `Game destroyed` are observed; the lifecycle is not error-free.
- The live terminal snapshot was captured approximately 93.851 ms after the marker. The private monitor recording missed early loading following a monitor-index failure. Preserve both provenance limits and the full log.
- This paragraph describes v1. The saved v2 repeat above extends the bounded hold evidence. Startup geometry differed, so the earlier startup improvement cannot be attributed to the arrival-only cap change. Actual human control, broader repeatability and unpaved terrain remain separate gates.

After explicit resumption, `.cache/client/runs/resume-status-radius5-v2/` was reconciled and closed normally. It failed during test-lead seat transfer before the new Resume command or 100-second no-getter episode. The lead was settled beforehand, but releasing native Wait one poll before pilot exit left a control gap: speed reached 2.32944 km/h and later displacement 15.10 m. This is a fixture failure, not failed authoritative Resume waiting. The report retains nine runtime and 64 shutdown errors and identifies the snapshot as a late first observation, not a contemporaneous terminal capture.

The v3 subclass retains native Wait for three stable samples, releases/exits in one call, and maintains an explicitly owned lead-only parking lease across seat transfer. The lease ends at native-hold handoff, powered movement, failure or deletion; follower controls remain untouched. All physical gates remain. Frozen `convoy-resume-seat-v3/data.pak` SHA-256 is `E4671BAE3CC73152785CB89D810B6908AA079F4D1634A6BBE3485DA91983E893`. The completed `.cache/client/runs/resume-status-radius5-v3/result.md` records 100.397 seconds stationary and 104.414 seconds without fixture getter polling, followed by production verified progress before completion and an independent 29.1049 m / 16.2655 m powered restart. Post-terminal observation lasted 30.148 seconds: maximum lead drift 0.0308848 m, follower zero. Later telemetry stayed stationary through approximately 429 seconds after terminal. Whole-course peak 67.446 m still fails 60 m; the new status episode alone peaked at 37.8201 m. Strict errors remain nine runtime and 64 shutdown, with normal cleanup. The first-result capture was 234 ms after the marker, and the private recording lasts approximately 691.667 seconds. No human-input, multiplayer or unpaved claim follows.

## Ordinary input diagnostic and panel correction

The later map-free `.cache/client/runs/input-pilot-mapfree-v1/result.md` uses `Worlds/Tests/ConvoyFollower_Arland_OpenRoad_InputPilot.ent` on frozen package **`C54DF7B703C915712ABE85F14A82E738C05A420D22B5BB7757DF4CC11500D801`**, changing the fixture's automatic map-opening option only. Up/M/interior-click/Up attempts produced no sampled raw key/button or mapped-action response. All 401 post-ready samples retained the original pilot, AI off, no map/editor/menu and enabled movement/view controls; all 20 routing samples had thrust/steering/map actions active. A late engine-cursor change is recorded, but cannot be attributed to the earlier click or treated as raw key/button calibration. No OSK-removal comparison occurred. Normal closure retains **nine runtime and 64 shutdown errors**; the private recording is 126.933333 seconds. The two native-map errors were absent, so neither those errors nor map-open action suppression is required for the observed input failure. Shared delivery/focus/device causes remain unresolved; no ordinary-input or driving pass follows.

The later read-only input/path observer package passed **WORKBENCH, PC, XBOX, PS4 and PS5** validation and packaging in `.cache/workbench-runs/convoy-path-input-observer-v2/`; data SHA-256 is **`8C58BE9E5458F7597F86F0EB4E7F233B92EF7CA00CBEB88D372E323C3560227B`**. Its completed `.cache/client/runs/panel-raw-input-v1/result.md` and `strict-evidence.json` record **no ordinary-input pass**: Hold click, short drag, Close Map and Escape produced zero raw changes, widget callbacks, orders or map closure. All 447 post-map samples retained the original pilot; sampled engine cursor position stayed 640,360. Bindings resolved and buttons had handlers; no diagnostic budget was exhausted. Up/M were not delivered. Normal closure retains **11 runtime and 62 shutdown errors**, with no slot errors.

The raw getters lack a positive runtime calibration here, so zeros establish neither a retail no-op nor absent physical input. The no-addon vanilla GM comparison also lacked native UI effects, but was not a native MapMenu/raw-observer comparison. Screenshot pointer changes alone do not prove engine hover. A positive ordinary-input control remains needed before attributing failure to production panel code; its resolver remains unexercised. Installed native hint-text and mask-name inconsistencies are recorded separately in the run's `native-map-errors.md`; they remain unpatched and are not established input causes. Full logs and private 164.53-second video are preserved. The earlier experiments below remain historical evidence.

The same frozen pack ran `Worlds/Tests/ConvoyFollower_Arland_OpenRoad_PanelPilot.ent` with read-only action/context/identity telemetry in `.cache/client/runs/panel-input-observer-v1/`. The actual Close Map click returned the player view, cleared native map/menu/editor state, and restored movement/view enable flags while CarContext remained active. All 757 settled samples retained the exact original player-pilot identity; AI activation stayed off. Supported desktop M/W/A taps produced no mapped action sample; Escape produced no menu transition. Initial 0.074 m settling was backward and unpowered. This rejects the active-AI/lingering-menu explanations for this baseline without proving a specific input-delivery cause.

The earlier input run retains **37 runtime / 64 shutdown errors**, with normal close. Twenty-six runtime errors came from addon button labels passed to FrameSlot APIs despite having ButtonWidgetSlot. The narrow correction uses installed ButtonSlot padding/stretch alignment, preserving centered labels and parent geometry. It passed five-configuration validation with the new paced fixtures. In the later `panel-input-special-key-v2` live run on frozen package `E85212EA74C18CE20C1190666DFD1DDE75BB2C0C0260BB6F3EABC7918E6C8A41`, labels rendered centered and readable with **zero slot errors**. Its **11 runtime errors** retain nine baseline signatures and two native map layout/mouse-mask errors. The 300-second launcher force fallback left normal shutdown unobserved; zero observed shutdown errors is not a clean lifecycle. OSK remained elevated, and its causal role is unproved. No keyboard restoration or sustained driving pass is claimed. Full logs, exact external input times, independent analysis and private recordings are preserved.

The later panel run also observed initial window placement consistent with requested client content at `(640,680)` (captured outer bounds `(639,649)`, `1282 × 752`). Attempted Hold click at 08:39:08.174 UTC and fresh activation at 08:39:18.786 failed with `failed to activate captured window`; uncertain input stopped. Activation of an existing neutral Notepad at 08:41:20.710 and fresh game activation at 08:41:31.975 succeeded without editing Notepad. Hold clicks at 08:41:47.907–47.990 and 08:42:13.162 produced no order event or feedback. Close Map at 08:42:30.197 highlighted the button but left the map open; the planned Up key was never delivered. All 874 post-open observer samples remained map-open with the original player seated, zero mapped driving action and no powered movement. The closing Alt+F4 attempt failed near the timer. `.cache/client/runs/panel-input-special-key-v2/result.md` and `strict-evidence.json` preserve the full result. A separate source candidate adds guarded own-button event normalization and bounded down/up/click diagnostics; it is not a verified fix. The generic [window options](client-window-options.md) are mirrored in the harness; unfocused `-forceUpdate` timing remains unverified.

The completed `.cache/client/runs/panel-input-events-v3/result.md` and `strict-evidence.json` record **no panel/input pass** on `C657779A...`. Hold, Close Map, Escape, a Close double click, native map toolbar and wheel attempts returned to the operator but produced **zero panel down/up/click callbacks, zero order requests and no map closure**. All 668 post-open samples retained the original player pilot and open map; action extrema and powered movement stayed zero. The trace budget was not consumed. Up/M were not delivered because the map-close prerequisite failed. This does not establish a resolver defect or a specific UI/input cause; the resolver was never exercised. Source inspection found no test-only action/control writes after initial setup. Subsequent raw-state, routing/binding and map-free observations also failed to establish ordinary input; the saved vanilla comparison used the native Game Master toolbar, not an opened MapMenu. Preserve those later results when selecting the next input hypothesis.

Root's Alt+F4 at 09:07:48.540 UTC reached cleanup at 04:07:48.925 and Game destroyed at 04:07:49.189 console time. **11 runtime and 64 shutdown errors** remain; slot errors are absent. The full log is 1,467,932 bytes, SHA-256 `EC4F158D709F327EC65AFEF715518C363822B57ED9B997106C558067E8CFBD32`. The private recording contains 3,150 frames / 210 seconds and has not been independently replayed here. OS-level close success does not prove game action delivery.

## Latest paced two-follower comparison

The newest combined-package driving comparison is recorded above. The following retains the earlier `E85212EA...` baseline.

The frozen package above ran `Worlds/Tests/ConvoyFollower_Arland_OpenRoad_Paced_2Trucks.ent`, with full artifacts in `.cache/client/runs/paced-road-2truck-v1/`. `PACED_RESULT` reported **FAIL** at 03:29:38.115, tick 121, elapsed 121.033 seconds. Whole-bound-run peak gaps were **66.4234 m / 230.336 m**, including setup and the start barrier. The legacy route gate also failed on 22 warning seconds despite settled arrival; the required 180-second all-truck observation never began. Original truck, pilot and actual predecessor identities remained intact after driving began, with independent powered-sample counts **26 / 33 / 38** for lead and the two followers. Travel and intact identity do not excuse the failed spacing/route gates.

Moving wheel samples report known `dirt_road` on all three vehicles, `grass_lush_tall` on the followers, and asphalt later. This is evidence for a mixed-surface mapped-road course, not a wholly unpaved or off-network run. The completed `paced-independent-result.md` and `paced-independent-analysis.json` retain nine runtime errors and no observed post-result/shutdown errors; the 540-second force fallback left normal lifecycle unobserved. Supplemental five-second position samples span 400.215 seconds with zero drift at logged precision on every truck, but cannot replace the missing formal identity/native-Wait observation or rescue the failed course. The requested 25 km/h cap was not a measured hard bound: lead speed reached 31.9629 km/h. The startup timeline establishes explicit arrival-hold clearing of both initial MOVEs, not native completion. The three-follower world is compiled but has no result here. The final fixture source captures peak gaps from original binding, uses read-only actual-target observation, requires 180 seconds and 180 samples with every truck within 2 m before a final PASS, and retains its owned native lead Wait until deletion.

## Preserved moving-restart checkpoint

These results and files predate this documentation reconciliation; no build or test was started for setup.

- **Frozen candidate:** `.cache/packed/convoy-moving-restart-v1/data.pak`, SHA-256 `652C4BE39007C0E9FF4F9175A3FB48D835FD3555A4A13D6B289F52AE8FFE6759`. Existing logs in `.cache/workbench-runs/convoy-moving-restart-v1/` confirm successful validation in WORKBENCH, PC, XBOX, PS4, and PS5, then successful packaging. The deployed package includes matching project and resource-database files.
- **Existing moving-restart run:** `.cache/client/runs/moving-restart-radius5-v1/`. Settings remain completion radius 5, moving gap 20, stopped gap 10, and production native cruise enabled. The saved terminal marker reports a seated chain, 117.954 m lead path, 128.236 m follower path, and 11.7298 m final gap; its observation marker records 30 seconds with `failed=false`. Independent saved timing analysis found a 58.6028 m link sample, slightly above the probe's 58.5319 m maximum. Retain the larger value and unchanged gate.
- **Startup interpretation:** first missing-MOVE reissue moved from +12.184 s in the boarding-handover baseline to +8.149 s, but that replacement also disappeared and required another request at +10.167 s. The first active update was +13.183 s. This is a promising single comparison, not proof that movement continuity or repeated starts are solved.
- **Later limitation:** the completed independent review measures 3.6998 m follower drift, first exceeding 2 m at 44.901 seconds after terminal. Lead position stays fixed. All 289 later cruise readbacks show brake 1/throttle 0, with the arrival latch and no waypoint still present. Drift began while cruise already requested zero, before the later positive cap; correcting that conflict alone is not proof of a complete fix. The strict review is complete, with lifecycle unobserved.
- **Capture provenance:** `gameplay-at-terminal.log` was actually captured after the result marker, following a failed sharing-mode read. `terminal-snapshot-provenance.json` records that method; do not describe it as an exact live capture at the terminal. Full logs and the existing private recording remain separate evidence.
- **New source:** preserve the compiled [control-handover candidate](convoy-control-handover.md), [authoritative Resume-status candidate](convoy-resume-status.md), production pacing, and completed explicit-Hold reporter. No possession or explicit panel commands occurred in the moving-restart comparison. The later v3 run verified a narrow authoritative Resume-status episode; possession, human input and other command branches remain separate physical gates. The newer `CF_ResumeStatusProbeComponent` and world/layer were included in later packs; they do not belong to the older moving-restart package. Keep their existing source and run artifacts.

The complete moving-restart review is preserved in its `result.md`, `strict-full-report.json`, and `late-drift-evidence.json`. The completed arrival-zero comparisons and targeted Resume-status v3 review above supersede earlier pending labels. Retain those results and the completed paced-course review. Under the current continuation, validate possession and remaining command branches separately. Do not repeat completed historical experiments merely because the brief describes their earlier status.

## Earlier evidence preserved through setup

The existing `.cache/client/runs/native-lead-hold-v2/` log and stored report corroborate a test-owned native wait: 30 seconds of first hold, powered lead restart, follower restart, and a second 30-second hold followed by 30 more seconds of observation, all with zero measured lead drift. Signed restart progress was 24.502 m for the independent lead gate; the combined marker later recorded 39.9711 m lead and 17.8267 m follower progress. This is a narrower fixture success, **not** a production hold or release pass.

The same run still fails the unchanged 60 m following-gap gate: peak separation was **75.712 m**. The updated stored report includes nine runtime errors, adding the previously omitted GUI error. Its timed stop has no `WORLD_CLEANUP` or `Game destroyed` marker: lifecycle remains **unobserved**, not clean. Preserve the original terminal snapshot and full console.

The same-pack radius-five comparison is already complete in `.cache/client/runs/native-lead-hold-radius5-v2/result.md`. With moving gap 20 and stopped gap 10, it passed the narrow sustained-hold/restart gates but still failed following: **65.0769 m** independently observed peak gap, against the unchanged 60 m gate. First hold lasted 30 seconds; the second lasted 30 seconds plus 30 seconds of post-result observation, with 0.00274319 m maximum reported drift. Follower restart progress was 21.7137 m. Normal close reached cleanup and `Game destroyed`, while retaining **64 shutdown error lines**. No unpaved or release claim follows from this concrete-taxiway result.

The saved comparison identifies earlier follower startup with radius five, followed by native braking at the lead's old starting point and a later MOVE rebuild. It does not establish that waypoint-origin updates fail to propagate. Later direct diagnostics below found no issued origin update before that first rebuild; exact completion-versus-update eligibility ordering remains unresolved. Keep each run's exact settings and recorded timeline.

Reinspection of the saved vanilla control now matches **all nine runtime resource/signature pairs**: GUI `SCR_WidgetExportRuleRoot`, M151A2 `SlidingTrackMaterial`, two BRDM2 `Parent`, four BTR70 `Parent`, and Helipad `m_bShowDebugShape`. The line mapping is in `.cache/client/runs/native-lead-hold-v2/load-error-investigation.md`. This extends the earlier Helipad-only finding without waiving errors or proving harmlessness. The saved comparison notes report 47 focused reporter tests and typecheck passed; these were not rerun during setup.

The frozen v2 pack is `.cache/packed/convoy-native-lead-hold-v2`, SHA-256 `D17AEB665902501A629D735466FA67E3982891D532FCB24E389D1D65C8C6CA50`. The [native-hold investigation](convoy-native-hold-investigation.md) records behavior ownership and subsequent fixture safeguards absent from this pack. Both radius settings used this same package.

Newer working source contains [persistent explicit Hold intent](convoy-control-followup.md), [production native cruise integration](convoy-native-cruise-integration.md), and direct `FOLLOW_MOVE_UPDATED` diagnostics. The taxiway follower no longer has the test-only cruise writer. Stored logs under `.cache/workbench-runs/convoy-production-cruise-v1/` confirm five-configuration validation and packaging; `.cache/packed/convoy-production-cruise-v1/data.pak` hashes to `8357AACE82D0817F2A194A488D93BBF2132C2BABA641ED2E024876430B1D766B`. Production Hold/rechain/seat-recovery regressions and the independent route-helper adapter remain unverified. Preserve modified source, worlds, resource database, reporter/tests, and independent harness work; do not reset to the older quoted commit.

## Latest saved comparisons and remaining validation

Earlier comparisons are preserved alongside the new boarding-handover run after resumption. Each result applies to its exact frozen package and exercised behavior.

- **Incomplete production deployment v1:** `production-cruise-radius5-v1` omitted `resourceDatabase.rdb` and produced extra addon GUID/name, replication, and network errors. It is invalid for driving comparisons. A complete frozen deployment needs matching `addon.gproj`, `data.pak`, and `resourceDatabase.rdb`. The newer launcher preflight rejects a missing/empty database; retain that existing tooling work.
- **Production cruise v2:** `.cache/client/runs/production-cruise-radius5-v2/result.md` records matching deployment files, the ordinary production pacing writer, 30-second first hold, powered restart, and a second 30-second hold plus 30 seconds of observation. Maximum second-hold lead drift was 0.0665847 m; follower restart progress was 20.7557 m. Peak link gap **64.3798 m** still fails the unchanged 60 m gate. Nine vanilla-matched runtime errors and 64 shutdown errors remain visible. Normal close reached cleanup and `Game destroyed`. This proves a narrow production-pacing/fixture-hold result, not player-commanded Hold or release readiness.
- **Moving refresh v1:** `.cache/client/runs/moving-refresh-radius5-v1/result.md` records a complete deployment but driver-seat loss before powered travel or any target update. The missing database therefore cannot be the sole explanation for seat loss. The new refresh branch was not exercised. Nine runtime and 62 shutdown errors are retained, with observed normal cleanup.
- **Moving refresh v2:** `.cache/client/runs/moving-refresh-radius5-v2/result.md` records the same immutable candidate's completed repeat. It passes 30-second first hold, powered lead/follower restart, second hold and 30 seconds of post-result observation, with 0.114222 m maximum lead drift. The strict peak link gap is **67.396 m**; separate camera telemetry observed 67.523 m. Nine runtime and 64 shutdown errors remain. Full logs, terminal snapshot, `timing-comparison.json`, and `startup-telemetry.md` are preserved. This is not evidence that reducing the moving-target refresh distance to 2 m improves following.
- **Timing limit:** in the completed refresh repeat, waypoint disappearance is bounded between the last present sample at +5.082 s and the first absent sample at +10.166 s. Lead displacement crossed 2 m between +6.999 and +7.999 s, within that interval. Rebuild occurred at +11.182 s; the first direct update was at +14.199 s. The five-second status throttle and missing per-poll eligibility values prevent establishing the exact order of native completion and refresh eligibility. Preserve that uncertainty when selecting the next diagnostic.
- **Boarding handover v1:** `.cache/client/runs/board-handover-radius5-v1/result.md` records a matching three-file deployment of the new guard/diagnostic pack. `BOARD_HANDOVER` waited with getting_in=true until ready=true three seconds later, then issued MOVE. No seat loss occurred in this fresh start. First hold lasted 30 seconds with 0.00012207 m maximum drift; lead restart reached 25.769 m with five powered samples, follower restart 21.818 m. Second hold lasted 30 seconds plus 30 seconds after terminal, with zero drift, through fixture second 137. Peak **70.0116 m** still fails, independently corroborated by 70.0034 m from rounded paired positions. Nine runtime and 64 shutdown errors remain; normal cleanup/destroyed are logged. This is a narrow handover/fixture-hold result, not a repeatability or player-command Hold pass.
- **New eligibility evidence:** restored 8 m refresh policy logged waypoint present at +7.133 s with advance 0.729 m, absent at +8.134 s with advance 4.199 m, then absent at the first sampled advance above 8 m (+9.150 s, 10.1185 m). No initial active update was eligible in the recorded polls. Rebuild occurred at +12.184 s, gap 38.2242 m; the first eligible active refresh/update followed at +15.217 s. Exact removal time remains bounded by adjacent samples; this does not resolve the earlier 2 m candidate's counterfactual.
- **Explicit Hold independent report complete:** `.cache/client/runs/explicit-hold-radius5-v1/result.md` and `strict-full-report.json` classify a narrow physical PASS: accepted server Hold, 30.1329 seconds with zero measured drift/speed while the lead advanced 40.1952 m, retained driver/truck/roster identity, then accepted Resume and 36.3685 m follower progress with two powered samples. The strict full run FAILS with nine vanilla-matched runtime errors and 64 shutdown errors; none are waived. Cleanup and `Game destroyed` are observed. The terminal prefix was reconstructed from the complete log afterward, explicitly recorded in `terminal-snapshot-provenance.json`; it was not captured live at the marker. Automatic post-terminal physical coverage is absent. Root's separate visual observation and reviewed recording samples do not replace that gate. Fixture lead-seat transfers do not prove ordinary menu input or human throttle/steering. Resume reports completion prematurely on acceptance in this pack; the status fix is a newer source candidate. The frozen package is unchanged.

Continue from source-to-pack provenance and the boarding/waypoint ownership evidence. Retain the failed 2 m candidate without promoting it solely because it compiled. The explicit Hold evidence review is complete; rechain/seat-recovery Hold regressions, safe control ownership on player possession, ordinary input, and the physical route-helper comparison remain open.

The previously recorded complete TypeScript suite passed **157 tests in 16 files**, and typecheck passed. The explicit-Hold reporter has 27 focused tests covering physical evidence, identity, ordering, and failure classification. These were not rerun during setup. The preserved moving-restart candidate adds a moving-predecessor missing-MOVE restart opportunity, retains 8 m / 3 s active refresh and the existing fallback, and requires an owned native cruise cap. Its hypothesis and unchanged gates are in `.cache/client/runs/moving-restart-radius5-v1/experiment.md`; the completed review and subsequent arrival-zero comparison are recorded above.

The brief's quoted commits are historical: the observed newer mod HEAD is `0d5ea95`, and the independent harness is at `3cdcf68`. The later default-off stable-waypoint source passed five-configuration validation and packaging, then failed its controlled comparison as recorded above; it remains unpromoted. Preserve any newer working-tree changes separately from that frozen package. Neither HEAD alone describes the modified mod working tree. Preserve newer source, fixture drafts, reports, and packages. The first-marker log snapshot helper's recorded 96-test/typecheck/build result remains existing evidence. The subsequent window-options implementation passed 24 focused tests and typecheck in each repository; those accepted checks were not rerun for this checkpoint. Harness commits `047a23b`, `9281db9` and `f9bb010` contain only the scoped launcher options, focused tests and documentation, including observed placement and unverified unfocused timing. This documentation reconciliation preserves pre-existing compiled and live evidence without rerunning it. The latest setup-only instruction authorizes no implementation, builds, tests, game launches, repository synchronization or goal transition; further work awaits the user's update and resumption.

## Test selection

Use the [short feature courses](convoy-feature-courses.md) first for individual controls, terrain, and parking faults. Preserve full Arland/Everon routes for final integration. A short fixture must exercise the real controller and measure movement; geometry and accepted orders have separate results.

The [adaptive roadmap](convoy-development-reset.md) prioritizes dependable movement and a complete basic supply trip as intermediate milestones. Assignment, individual commands, recovery, arrival, regrouping, return, multiplayer, and release polish remain in scope. Sequence their tests around established foundations; deferred work is not removed from the product goal.

## Evidence

The table below retains the earlier checkpoint results. The newer native-hold result above supersedes the unresolved fixture-hold status, while its overall following failure and remaining product gates stay open.

| Check | Result and limit |
| --- | --- |
| Native road geometry | 18 cases passed in GAME, including road walking, bends, angled arrivals, and a blocked corridor. |
| One/two/three-truck road following | Strict passes exist at the preceding packed checkpoint. They do not establish all terrain or current-build regression coverage. |
| Stop, exit, reboard, resume | A prior two-truck scripted AI-lead run passed. Actual player input and map-panel focus remain separate gates. |
| Revised forward lane check | Blocked-lane gameplay gate passed on the new pack: correct refusal, driver seated, 0.033 m motion in the hold. |
| Road81 three-truck forward sequence v5 | Failed. Road arrival passed; Unit One physically drove about 89 m ahead. Its brake capture occurred near the slot, but it subsequently moved backward before parking completed. Unit Two correctly did not advance. No owner pass or resume was tested. |
| Short arrival one-truck v1 | Fixture failed before convoy driving: scripted owner lead moved only 0.053 m in 75 seconds. Its neutral gear and unsigned-motion assumption were corrected in later tests. |
| Short arrival one-truck v2 | Fixture still failed with forward gear 2 and clutch 1: 1,206 callbacks, throttle 0.28, brake 0, engine RPM 600, but only 0.021 m forward progress. All six wheels reported asphalt. The separate calibration below failed too; this scripted-owner branch and the two-truck course are held. Core navigation tests use the working AI lead. |
| Isolated forward parking v1 | Fixture never reached release readiness: stationary truck centers were 14.06 m apart, just outside the 14 m gate, with no previously driven trail. No release order was issued. The v2 fixture moved the follower start 2 m farther along the measured road; production readiness thresholds were unchanged. |
| Isolated forward parking v2 | Physical PASS on frozen v4: 87.42 m traveled, 87.27 m displacement, seated driver, 1.68 m road gap, then 20 seconds parked with zero measured drift. Actual brake/handbrake stayed applied. This does not prove the larger multi-truck release or owner-pass sequence; the parked truck was partly tree-obscured in the camera view. |
| Owner-drive calibration v2 | Strict FAIL. Three POSTFRAME throttle levels made no forward progress. Pre/post-physics samples caught clutch 0, throttle 0, brake 1 despite the writes. Physics callbacks then stopped; later SIMULATE modes never exercised their write branch. |
| Owner-drive calibration v3 | Strict FAIL after explicit ACTIVE wakes at the two SIMULATE stage entries. Physics ran for 1,280 callbacks, but PRE reads still showed throttle 0/brake 1 while POST reads showed the requested 0.5/0. Signed motion was −0.228 m over 40 seconds. Awake physics and pilot locking did not establish powered forward control. This branch is stopped. |
| Session shutdown guard | Both v3 runs logged one detached session before world destruction, with no subsequent convoy AI orders. Base-game resupply/UI shutdown errors remain. Same-process world restart and in-game member removal are separate regression gates. |
| Everon field following | Fixture survey failed: fifteen 60–100 m candidates were rejected by shrubs/wall before any convoy order. No offroad movement was tested; a clearer field is being selected. |
| Arland clear-field survey | Two 80 m corridors passed terrain/obstacle/road-distance geometry checks. The later live run revealed candidate 120 crosses an airfield taxiway. Distance from mapped roads did not establish unpaved terrain. |
| Arland taxiway following v1 | Physical completion, **strict FAIL**. Lead path 76.04 m, follower path 86.93 m, final gap 7.46 m; peak gap reached 91.17 m while the follower waited, then caught up. Video and lead wheel contacts show concrete, with some grass contacts. The original probe printed PASS, but it is not a product pass or off-road proof. The strict report rejects the base Helipad load error (`m_bShowDebugShape`); the strengthened reporter also rejects peak gap >60 m and lacks natural-surface evidence for both trucks. |
| Ordinary off-network arrival correction / taxiway v2 | All five Workbench configurations and pack passed. Live comparison removed the false road blocker (zero `ARRIVAL_ROAD_BLOCKED`) and reached `ARRIVAL_TRAIL_HOLD`; final roster correctly said holding on driven route. Startup lag remained nine seconds and peak gap 92.03 m, so strict following still failed. Final gap was 7.08 m after a very close angled stop and creep. Instrumentation showed initial MOVE consumed at 20 m, engine off; the later rebuild waited for gap >30 m, followed by native engine startup. Road parking/release gates remain unchanged. |
| Engine preparation / taxiway v3 | A single ordinary `StartEngine()` request after confirmed boarding was accepted and the engine started before the later MOVE. Follower motion began at second 13 versus 17; peak gap improved from 92.03 to 71.00 m, still outside the 60 m gate. Terminal FAIL after 180 seconds: too-close angled arrival and lead backward drift, goal gap 11.68 m, link gap 6.23 m. Drift also exposed another stale-policy road-blocking transition. Early pacing and that state transition are the next bounded fixes. |
| Native cruise experiment v1 | Six speed-policy cases passed; native movement ownership resolved to the assigned truck. Actual throttle fell and brakes applied as the requested cruise speed fell. Follower stopped about 16.4 m behind the lead. The lead then independently reversed while the follower remained stationary. Strict FAIL: peak gap 70.89 m, goal gap 18.55 m, no settled terminal result. This pacing component is attached only to the test follower. |
| Native cruise plus lead-hold experiment | Requesting cruise zero on the test lead produced a ten-second settled result: 15.66 m final gap, 5.85 m goal gap. **Not a sustained-hold pass:** video and full logs show the lead reversing again 8–9 seconds after the nominal PASS marker while the follower stayed still. Peak separation remained 70.08 m. Overall strict FAIL; preserve the post-terminal footage, not just the early marker. |
| Separate waypoint geometry setting | `m_fMoveCompletionRadius` defaults to zero, preserving the old moving-gap radius. At the older pause it had only compiled/packed. The later same-pack radius-five physical comparison above has now run and still fails spacing; it is not final tuning. |
| Independent driven-route helper | All 25 native geometry cases passed in GAME on guidance-v1, after five-configuration compile and pack. Physical adapter comparison remains pending. It is not wired into production driving. |
| UI | Prior filmed map panel accepted Hold/Resume and displayed server completion. Direct map-key opening and restored throttle/steering after close are not yet proved. |

Current live logs:

- Native hold baseline and final classification: `.cache/client/runs/native-lead-hold-v2/`.
- Same-pack radius-five comparison and timeline: `.cache/client/runs/native-lead-hold-radius5-v2/`.
- Invalid incomplete deployment: `.cache/client/runs/production-cruise-radius5-v1/`.
- Completed production integration comparison: `.cache/client/runs/production-cruise-radius5-v2/`.
- Complete-package startup seat loss: `.cache/client/runs/moving-refresh-radius5-v1/`.
- Completed moving-refresh repeat and bounded startup telemetry: `.cache/client/runs/moving-refresh-radius5-v2/`.
- Boarding-handover comparison: `.cache/client/runs/board-handover-radius5-v1/`.
- Explicit Hold/Resume run, completed strict review: `.cache/client/runs/explicit-hold-radius5-v1/`.
- Moving-restart candidate, completed report and later creep: `.cache/client/runs/moving-restart-radius5-v1/`.
- Arrival-zero candidate, bounded physical PASS and strict FAIL: `.cache/client/runs/arrival-zero-radius5-v1/`.
- Arrival-zero repeat, bounded physical PASS and strict FAIL: `.cache/client/runs/arrival-zero-radius5-v2/`.
- Resume-status v2, reviewed test-lead seat-transfer failure: `.cache/client/runs/resume-status-radius5-v2/`.
- Resume-status v3, completed targeted status PASS with whole-course spacing and strict error FAIL: `.cache/client/runs/resume-status-radius5-v3/`.
- Geometry: `.cache/workbench-gui-runs/2026-09-26T02-18-48-305Z-10812/console.log`.
- Blocked lane: `.cache/client/runs/corridor-v1-forward-blocked/`.
- Three-truck forward attempt: `.cache/client/runs/road81-v5-3truck/`.
- Short arrival: `.cache/client/runs/short-arrival-1truck-v1/`.
- Gear-corrected short arrival: `.cache/client/runs/short-arrival-1truck-v2/`.
- Isolated parking readiness failure: `.cache/client/runs/forward-slot-1truck-v1/`.
- Isolated parking physical pass: `.cache/client/runs/forward-slot-1truck-v2/`.
- Owner control calibration: `.cache/client/runs/owner-drive-calibration-v2/`.
- Explicit-wake calibration: `.cache/client/runs/owner-drive-calibration-v3/`.
- Clear-field geometry: `.cache/client/runs/arland-clear-field-survey-v1/`.
- Off-network arrival comparison: `.cache/client/runs/arland-clear-field-1truck-v2/`.
- Engine preparation comparison: `.cache/client/runs/arland-clear-field-1truck-v3/`.
- Vanilla helipad control: `.cache/client/runs/vanilla-gm-arland-helipad-baseline/`.
- Cruise pacing: `.cache/client/runs/arland-clear-field-cruise-v1/`.
- Cruise plus lead hold: `.cache/client/runs/arland-clear-field-cruise-hold-v1/`, including `corrected-result.md` and `strict-full-report.json`.
- Taxiway physical diagnostic: `.cache/client/runs/arland-clear-field-1truck-v1/`.
- Independent route cases: `.cache/client/runs/driven-route-geometry-v1/`.
- Rejected Everon field: `.cache/client/runs/everon-offroad-1truck-v1/`.

The client runs retain a `gameplay-at-terminal.log` snapshot and the complete `logs/console.log`. The gameplay snapshot bounds the scenario result; the complete log also contains shutdown errors. These are separate findings, and a passing snapshot must not be described as an error-free full lifecycle. See [lifecycle review](convoy-lifecycle-review.md) for the reproduced base-game shutdown diagnostics and the separate convoy teardown fix.

The older owner-drive calibration accepted distance alone. Its lead moved downhill in the opposite direction to its facing, so that clip is not proof of powered forward driving. Future scripted-owner gates require progress in the intended direction and actual gear, clutch, brake, and throttle readings.

The 45-second vanilla Game Master Arland control loaded only `core` and `ArmaReforger`, reached GAME at 22:36:04.447, and reproduced the exact `m_bShowDebugShape` error at offset 2307 while loading base `Helipad_Lights_US_01.et` at 22:36:04.806. This identifies that load error as independently reproduced without this addon; it remains visible in the strict report and is not a claim of an error-free game lifecycle.

[Taxiway follower-lag diagnostic clip](media/arland-paved-follower-lag-v1.mp4) preserves the failed baseline for comparison. It shows paved travel and a late catch-up, not a successful unpaved convoy run. The current terrain reporter defaults to `--surface unpaved`, requiring moving natural-material wheel samples from both trucks. `--surface off-network` labels an unmapped paved course without claiming unpaved ground; its other physical, 60 m peak-gap, and error gates still apply.

## Frozen builds

| Pack | SHA-256 of `data.pak` |
| --- | --- |
| `.cache/packed/convoy-road-corridor-v1` (blocked lane and road81 v5) | `49B2460259B48CA62F45A7CE9C69F30B51E9B46B5A18E9D87CF20FD32AD45302` |
| `.cache/packed/convoy-feature-courses-v2` (short arrival v1 and first field test) | `B55B50E3972264F7D210CB6DF6F0B67EF45E1342133AFE9EF17B208D14C43FA0` |
| `.cache/packed/convoy-feature-courses-v3` (short arrival v2 and isolated parking v1) | `7700B4B063929105D68C590B0CC6B316B736256479E04260CADEC3F3ECAE69DF` |
| `.cache/packed/convoy-feature-courses-v4` (parking v2, calibration v2, field survey) | `28ECA5B662F1D8E9020821606A7ACA1816B9E855162749D60DF58220C89CE991` |
| `.cache/packed/convoy-guidance-v1` (independent route cases and wake diagnostic) | `27D2AF85070871A0013F91FD5514C51DC949E881B91E1723B888A05303398669` |
| `.cache/packed/convoy-clear-field-v1` (taxiway physical diagnostic) | `2507A0232756A0A8F52D3701FA9CD9F9A0A0636C195DE64380DA2F660FECA756` |
| `.cache/packed/convoy-offnetwork-arrival-v1` (ordinary arrival correction) | `783B9A89D036E711E3A0E952F3CF4B808FB5C43318C22AE1924719857591A18E` |
| `.cache/packed/convoy-engine-ready-v1` (one-shot engine preparation) | `195A3E7DB0A0F68C23A334826D336A2E50BC5C0ED7516262F01C7AA1C14E6D05` |
| `.cache/packed/convoy-cruise-probe-v1` (test-only follower pacing) | `A64A1B6C391FF75E61757ADE18AB6FBEB3C324F9762FCE603A1A89D9A1DB1F5E` |
| `.cache/packed/convoy-cruise-lead-hold-v1` (test lead zero-cruise request) | `62AB8EFA25448E877B61C3A9B9B971CAD0EACA02F59743598BD39ADF85773526` |
| `.cache/packed/convoy-pause-checkpoint-v1` (geometry-radius setting remains disabled) | `19989419D4BA45498E4D6DA7203F9614E6A2084334F0EF6357FD80D1959517AA` |
| `.cache/packed/convoy-native-lead-hold-v2` (legacy/radius-five controlled comparison) | `D17AEB665902501A629D735466FA67E3982891D532FCB24E389D1D65C8C6CA50` |
| `.cache/packed/convoy-production-cruise-v1` (production pacing; completed v2 still fails spacing) | `8357AACE82D0817F2A194A488D93BBF2132C2BABA641ED2E024876430B1D766B` |
| `.cache/packed/convoy-moving-refresh-v1` (2 m candidate; startup failure and completed spacing failure) | `00FB22C5F930C8D100F7A7E8CEBD685C4B2981D6B3E001430DBB1FCDDBDB1D2A` |
| `.cache/packed/convoy-board-handover-v1` (guard observed once; spacing fails; separate explicit Hold physical PASS, strict FAIL) | `72553817EA40AF6AAC237D38CF1FB8F5719E363C358424EFA92B9FE4139BAF60` |
| `.cache/packed/convoy-moving-restart-v1` (bounded spacing pass; later creep; completed strict review fails) | `652C4BE39007C0E9FF4F9175A3FB48D835FD3555A4A13D6B289F52AE8FFE6759` |
| `.cache/packed/convoy-arrival-zero-v1` (two 180-second physical hold passes; strict runtime/lifecycle failures) | `30D094F57232348A4E891AA35FA46FF06D480A4CC20C7396CBCB605AB98C5225` |

These packs passed installed Workbench script validation in five configurations and resource packaging. The earlier surface and peak-gap reporter checkpoint passed all 95 TypeScript tests across 15 files and typecheck; the later 157-test record above supersedes that tooling count. These checks do not establish vehicle behavior.

Historical reporter checkpoint: 27 focused tests and typecheck passed, separating physical scenario, surface, runtime, and lifecycle results. At that point the two older cruise runs were reported with eight runtime errors, only Helipad was classified against vanilla, and their full logs retained 64 and 62 lifecycle errors respectively. The newer nine-error classification and 47-test result above supersede that coverage statement; the later runs use different frozen packages. None of these records is an error-free full-lifecycle claim.

## Repositories and release shape

The [Convoy Follower repository](https://github.com/MarcoBetti1/reforger-convoy-follower) owns addon code, audio, scenes, and gameplay evidence. The independent [Reforger Agent Harness](https://github.com/MarcoBetti1/reforger-agent-harness) owns generic launchers, Workbench workflow, and reusable operating lessons.

Keep a single Workshop addon while extracting the road math internally. A separate driving dependency requires a proven independent caller and terrain coverage first; see [the navigation plan](convoy-navigation-release-plan.md).
