# Autonomous convoy edge cases

This matrix adds repeatable scenes around the normal one-, two-, and three-truck road baseline. It distinguishes what a fixture **intends to test** from what a live F5 run has demonstrated. Use [the primary smoke guide](convoy-automated-smoke.md) for the full road probe, log parser, and recording workflow.

| Case | World | Intended observation | Present evidence |
| --- | --- | --- | --- |
| Tricky turn and gate | `Worlds/Tests/ConvoyFollower_Arland_Auto_1Truck.ent`, `_2Trucks.ent`, `_3Trucks.ent` | AI lead negotiates the Arland base exit; each follower stays behind its immediate predecessor without a sustained stall or order inversion. | A previous one-truck gate run passed. A prior two-truck run stalled Unit 1 near the gate; **rerun with the current build**. |
| Open road baseline | `Worlds/Tests/ConvoyFollower_Arland_OpenRoad_Auto_{1,2,3}Truck(s).ent` | One to three linked followers complete a connected hill-road route and settle on road in order for 20 seconds. | Earlier three-truck strict gate passed; current-build rerun is required. |
| Stop, exit, reboard, resume | `Worlds/Tests/ConvoyFollower_Arland_OpenRoad_Auto_2Trucks_PauseResume.ent` | A passenger owner rides while an AI pilot drives, the lead stops, the owner gets out, both drivers stay assigned and seated, the owner reboards, then the chain completes a second road leg. | **PASS on September 25 retest after trail-resync changes.** The owner exited, stayed on foot for 15 seconds, and reboarded. Both drivers kept their assignments and completed the second leg: lead 223.598 m, Unit 1 263.641 m, Unit 2 248.252 m. The road probe passed with final widest link gap 17.04 m; strict `--variant pause-resume --require-pass` exited 0. Peak en-route gap was 189.15 m for a short interval. The prior build failed this fixture when Unit 1 detoured and briefly left its seat, so retain it as a regression test. The wide side camera was active in the passing run. |
| Explicit unload and return | `Worlds/Tests/ConvoyFollower_Arland_OpenRoad_Auto_2Trucks_Release.ent` | Unit 1 parks clear, Unit 2 reaches the bay, owner orders a regroup, and both physically return in one chain. | The September 25 v15 F5 run **failed** the strict `--variant release` gate. Unit 1 physically cleared about 31.5 m from the bay but stopped roughly 16.7 m short of its validated rear waiting slot, then logged `UNLOAD_TURN_BLOCKED` and `UNLOAD_RELEASE_FAILED` at 09:42:27. Unit 2 therefore never advanced into the bay. This is a failed completion, not a parked return queue. |
| Forward wait, historical narrow road | `Worlds/Tests/ConvoyFollower_Arland_OpenRoad_Auto_2Trucks_ForwardWait.ent` | Unit 1 takes a distinct player-issued **Pull ahead and wait** order, stays seated, clears the unload bay, parks at a reachable forward slot, and Unit 2 advances. | **Historical V3 live F5 and strict gate PASS (September 25, before the road-width guard).** Road arrival passed after a 211.2 m lead path. Only the test lead was staged 10 m onto a shoulder. Unit 1 drove about 96 m, stayed seated, parked ahead, and Unit 2 reached the bay. The fixed film view shows departure and successor approach, not the far parked slot. A later road survey measured the bay and both forward slots at only **3.5 m nominal road width**. The current build now rejects forward passing below 8 m, so this is a checkpoint for prior behavior, **not** a current-build positive fixture. A current-build safe-width refusal must be tested separately. |
| Forward lane blocked | `Worlds/Tests/ConvoyFollower_Arland_OpenRoad_Auto_2Trucks_ForwardBlocked.ent` | With the lead still centered in the travel lane, the server rejects **Pull ahead and wait** for that specific obstruction; Unit 1 remains seated and stationary. | **September 25 live F5 and strict gate PASS.** Road arrival covered 211.901 m; strict parser confirmed correct world/GAME/two trucks, peak en-route gap 119.289 m, 0 s range warning, 3 s nonprogress, exact lane-obstruction rejection, Unit 1 still seated and only 0.054 m moved during a five-second hold, and zero relevant runtime errors. Log: `.cache/workbench-gui-runs/2026-09-25T16-13-18-060Z-24136/console.log`. The finalized [40-second video](media/convoy-forward-blocked-proof.mp4) shows the refusal. This proves a safe refusal, not a successful forward maneuver. |
| Two trucks ahead and explicit resume | `Worlds/Tests/ConvoyFollower_Arland_OpenRoad_Auto_3Trucks_ForwardTwoAhead.ent` | Units 1 and 2 park ahead on distinct connected road slots at least 16 m apart; Unit 3 reaches the bay. The owner lead physically drives past both, an explicit **Resume ahead line after passing** reconnects the original identities, and all three trucks move again. | **September 25 first F5 and strict gate FAIL (exit 1; zero relevant runtime errors).** The normal three-truck road arrival passed. Units 1 and 2 accepted separate forward orders, parked seated ahead about 19.49 m apart in correct order, and Unit 3 reached the bay. The test AI pilot then physically drove the owner's shoulder-staged lead vehicle, but oscillated behind the farthest parked truck and never crossed the forward line. The probe ended `AUTO_TWO_AHEAD_RESULT: FAIL` at 11:23:43; explicit resume was never issued. While the forward line was pending, active Unit 3 at the bay logged `DRIVE_STALL_CHECK` at 11:22:34/11:23:04/11:23:34, then `STUCK_TERMINAL` and `CONVOY_UNIT_REMOVED` at 11:23:34. That dismount is an independent failure in a waiting convoy. Log: `.cache/workbench-gui-runs/2026-09-25T16-18-24-324Z-4552/console.log`. The finalized 338.875 s video shows a fixed wide view of the road and open field, with trucks too small/partly obscured by trees to prove precise positions; the log/probe establishes the parking order and failed pass. |
| Width-8 road, two ahead and explicit resume | `Worlds/Tests/ConvoyFollower_Arland_WideRoad_Auto_3Trucks_ForwardTwoAhead.ent` | Repeat the same three-truck physical gate on surveyed road candidate 28, with a wider lane and passing corridor. | **V1 and V2 strict FAIL on September 25; no forward park or resume proved.** V1 outbound arrival passed (lead path 218.044 m; final widest gap 14.655 m; peak gap 126.454 m, warning 0 s, nonprogress 3 s). The test lead was shoulder-staged 9.13 m from road, but the first forward order was rejected because lead-facing 85 m projection `<1517.13,36.37,3099.04>` found no connected road point within 25 m. Log `.cache/workbench-gui-runs/2026-09-25T17-01-58-310Z-33620/console.log`; finalized diagnostic video `.cache/test-videos/auto-3truck-forward-twoahead-wide-v1.mp4` (198.625 s) is tree-obscured. Production then changed to a road-polyline arc target, and the isolated fixture camera moved to the opposite side. **V2 failed before reaching that order**: Unit One and Unit Two began 27.31 m and 15.79 m off the mapped road, delaying the daisy chain until en-route gap 261.579 m, range warning 8 s, nonprogress 24 s. `AUTO_RESULT: FAIL` and strict exit 1, zero relevant runtime errors; log `.cache/workbench-gui-runs/2026-09-25T17-09-46-167Z-28060/console.log`, finalized diagnostic video `.cache/test-videos/auto-3truck-forward-twoahead-wide-v2.mp4` (344.125 s). A read-only centerline survey and corrected scene staging are required before judging the arc target. The separate `--variant forward-twoahead-wide` gate still requires two physical parks, safe hold, owner pass, explicit resume, and all three moving. |
| Width-8 road, centered-staging v3 | Same isolated width-8 road world, restaged from a read-only 30 m arc-spacing survey | Establish a valid three-truck approach, then retain the first parked-ahead truck while the next reaches the bay. | **V3 outbound arrival PASS; full strict gate FAIL (September 25, exit 1; zero relevant runtime errors).** All trucks started on the mapped width-8 centerline: Unit 1/2/3 initial road gaps 0.356/0.00494/0.00234 m. Lead traveled 185.583 m, settled with widest gap 13.377 m, peak en-route gap 131.7 m, warning 0 s and nonprogress 4 s. The arc-based forward order was accepted at 12:25:14 for connected slot `<1528.31,37.24,3069.18>`. Unit 1 physically advanced about 88 m and logged `FORWARD_WAIT_PARKED` and bay clear at 12:25:29 while seated. Unit 2 reached within 7.47 m of the bay. Unit 1 then rolled about 1.5–2 m backward from its marginal 8.18 m slot gap to beyond the 9 m parked predicate, so that predicate stayed false and the second order never ran. `AUTO_TWO_AHEAD_RESULT: FAIL` at 12:27:15. Log `.cache/workbench-gui-runs/2026-09-25T17-22-23-958Z-35828/console.log`. This is a parking-stability failure; an event log saying parked once does not establish a persistent parking state. The wide camera is on the clearer side, but the final video still needs review before any visual claim. |
| Width-8 road, parking-stability v4 | Same centered width-8 world and three-truck probe | Hold the first truck in its forward slot while Unit 2 approaches, then select a distinct second forward slot. | **V4 outbound arrival PASS and first park stable; full strict gate FAIL (September 25, exit 1; zero relevant runtime errors).** Lead traveled 185.555 m, final widest link gap 13.284 m, peak en-route gap 115.67 m, warning 0 s, nonprogress 4 s. Unit 1 stopped at `<1522.54,36.82,3067.89>`, 4.63 m from its connected slot; diagnostic samples for 26 seconds showed drift 0, speed 0, brake and handbrake engaged, and `parked=true`. Unit 2 advanced to 9.44 m from the bay. The second planned slot `<1507.41,35.60,3066.84>` was only 15.17 m from Unit 1's **actual** parked position, so the server correctly rejected it under the 16 m separation rule. `AUTO_TWO_AHEAD_RESULT: FAIL second forward order rejected` at 12:38:16; explicit owner pass/resume did not run. Log `.cache/workbench-gui-runs/2026-09-25T17-34-46-900Z-32432/console.log`, finalized diagnostic video `.cache/test-videos/auto-3truck-forward-twoahead-wide-v4.mp4`. This run confirms the v3 drift was fixed for the first truck, but does not prove a two-truck forward line. |
| Width-8 road, parking-capture v5 | Same centered width-8 world and three-truck probe | Retest the first stable park with a more distant second slot. | **V5 outbound arrival PASS; full strict gate FAIL (September 25, exit 1; zero relevant runtime errors).** Lead traveled 184.779 m; final widest gap 13.121 m, peak en-route gap 127.81 m, warning 0 s, nonprogress 4 s. Unit 1 drove toward connected slot `<1526.70,37.09,3069.05>` but the AI completed its waypoint at `<1521.54,36.71,3067.25>`, 5.481 m short. That was just outside the new 5 m brake-capture radius, leaving no active waypoint and no validated parked state. Two route reissues produced no movement. After two minutes, the server logged `FORWARD_WAIT_BLOCKED` and `FORWARD_WAIT_FAILED`; the probe ended `AUTO_TWO_AHEAD_RESULT: FAIL` at 12:47:53. The second order and explicit resume never ran, so the wider second-slot plan remains untested. Log `.cache/workbench-gui-runs/2026-09-25T17-42-55-255Z-9552/console.log`, finalized diagnostic video `.cache/test-videos/auto-3truck-forward-twoahead-wide-v5.mp4`. |
| Width-8 road, shoulder-preflight v6 | Same centered width-8 world and three-truck probe | Establish the same wide-road arrival and stage a level shoulder so the forward orders can be retested. | **V6 outbound arrival PASS; forward test did not start (September 25, strict exit 1; zero relevant runtime errors).** Lead traveled 185.288 m, final widest gap 15.859 m, peak en-route gap 134.722 m, warning 0 s, nonprogress 5 s. The test probe rejected its moving-heading-based shoulder search at 12:53:49 with `FAIL no level, clear shoulder for test lead`; it issued neither forward order. This is a fixture-preparation failure, not evidence that the new production brake-capture or slot spacing failed. Log `.cache/workbench-gui-runs/2026-09-25T17-50-43-431Z-29688/console.log`, finalized diagnostic video `.cache/test-videos/auto-3truck-forward-twoahead-wide-v6.mp4`. A subsequent test-only change selects the previously measured candidate28 shoulder directly and logs the specific preflight reason on rejection. |
| Width-8 road, arrival-readiness v7 | Same centered width-8 world with fixed test-only shoulder target | Reach a three-truck stop that the production release command accepts, then test two forward slots. | **V7 road arrival gate FAIL before shoulder stage (September 25, strict exit 1; zero relevant runtime errors).** Lead traveled 186.441 m; final widest gap 16.901 m, peak en-route gap 129.469 m, warning 0 s, nonprogress 3 s. Unit 1 received `STOP_SETTLE_ROAD` toward mapped point `<1435.3,36.98,3048.08>`, but the AI completed the MOVE and settled 5.401 m from the nearest mapped road centerline, with NULL waypoint and brake engaged. The then-current 5 m threshold applied to both the smoke gate and production `CF_IsTruckNearMappedRoad`, so release readiness would also remain false in gameplay. `AUTO_RESULT: FAIL road arrival or stable chain`; neither forward order ran. Unit 1 and Unit 3 also briefly left their seats earlier in the route and recovered through reboard; that did not cause this final gate failure. Log `.cache/workbench-gui-runs/2026-09-25T17-57-50-956Z-31296/console.log`, finalized diagnostic video `.cache/test-videos/auto-3truck-forward-twoahead-wide-v7.mp4`. Production and probe now share a capped, road-width-aware allowance for the next retest; no v7 maneuver proof is claimed. |
| Width-8 road, two parks and owner pass v8 | Same centered width-8 world with fixed shoulder and road-width-aware arrival gate | Park two trucks ahead, bring Unit 3 to the unload bay, physically drive the lead past both, then explicitly resume all three. | **V8 route and both physical forward parks PASS; full strict gate FAIL (September 25, exit 1; zero relevant runtime errors).** Lead route 183.625 m, final widest link gap 12.983 m, peak en-route gap 127.561 m, warning 0 s, nonprogress 4 s. The first and second rear-menu-equivalent orders were accepted; Units 1 and 2 parked seated on connected width-8 road slots **26.35 m apart**, and Unit 3 reached the bay within 4.48 m. The test AI pilot then pursued one centerline MOVE and oscillated directly behind the nearer parked truck at x1482–1491; it never passed the farther truck. The video visibly shows the lead blocked behind Unit 2 and an apparently open south verge, though physical clearance there still needed testing. While original Unit 3 intentionally waited in the outbound bay, it also hit `STUCK_TERMINAL` at 13:14:04 and left the convoy: a separate false-stuck failure. `AUTO_TWO_AHEAD_RESULT: FAIL AI-piloted owner vehicle did not physically pass parked-ahead line` at 13:14:14. Strict parser reports **two-ahead physical parking PASS**, follower-hold FAIL, final sequence FAIL. Log `.cache/workbench-gui-runs/2026-09-25T18-08-36-919Z-6592/console.log`; finalized diagnostic video `.cache/test-videos/auto-3truck-forward-twoahead-wide-v8.mp4`. Subsequent test-only work replaces the AI centerline pass with a checked, physically driven side-lane bypass; production separately addresses the intentional-hold false-stuck event. Neither correction is live-verified yet. |
| Width-8 road, shoulder-side bypass v9 | Same centered width-8 world; test-only owner bypass preflight added | Keep two stable forward parks and bay access, then physically drive the owner around the parked line and resume the three-truck chain. | **V9 road arrival and two forward parks PASS; full strict gate FAIL (September 25, exit 1; zero relevant runtime errors).** Lead traveled 184.887 m; final widest link gap 13.372 m, en-route peak 121.983 m, warning 0 s, nonprogress 4 s. Units 1 and 2 parked seated about 26.10 m apart and Unit 3 reached the bay. The owner took the pilot seat, but the test-only bypass preflight rejected target 1 sample 4 as vehicle occupied; the owner did not pass the line and explicit resume was not issued. The bypass normal pointed to the side opposite the staged lead shoulder, so a corrected shoulder-side route awaits F5. `AUTO_TWO_AHEAD_RESULT: FAIL no clear physical owner-vehicle route past parked line` at 13:27:03. The production forward-outbound hold trigger needs more than 20 m owner movement and **was not exercised**, even though no pre-result `STUCK_TERMINAL` occurred. Log `.cache/workbench-gui-runs/2026-09-25T18-23-04-078Z-17676/console.log`; video `.cache/test-videos/auto-3truck-forward-twoahead-wide-v9.mp4`. |
| Width-8 road, traced south bypass v10 | Same centered width-8 world, with shoulder-side normal matched to staged lead | Keep two stable forward parks and bay access, then physically pass and explicitly resume. | **V10 route and both forward parks PASS; full strict gate FAIL (September 25, exit 1; zero relevant runtime errors).** Lead traveled 186.233 m, final widest link gap 16.991 m, en-route peak 159.120 m, warning 0 s, nonprogress 5 s. Units 1 and 2 parked seated 26.19 m apart and Unit 3 reached the bay within 5.80 m. The test owner took the lead driver seat. The corrected positive-Z shoulder-side route passed sampled grade, road proximity, and vehicle occupancy checks, but its first segment failed the entity trace at fraction 0.553. No physical owner pass, outbound-hold trigger, or explicit resume occurred. `AUTO_TWO_AHEAD_RESULT: FAIL no clear physical owner-vehicle route past parked line` at 13:34:03. The next test-only revision logs the trace hit and tries three bounded lateral offsets; production convoy behavior is unchanged. Log `.cache/workbench-gui-runs/2026-09-25T18-29-45-786Z-32900/console.log`; video `.cache/test-videos/auto-3truck-forward-twoahead-wide-v10.mp4`. |
| Width-8 road, obstacle-checked bypass v11 | Same centered width-8 world; three bounded south-side route candidates | Find a clear physical player-lead path around two parked trucks, then exercise outbound hold and explicit resume. | **V11 road arrival and two forward parks PASS; full strict gate FAIL (September 25, exit 1; zero relevant runtime errors).** Lead route 185.274 m, final widest gap 13.338 m, en-route peak 127.272 m, warning 0 s, nonprogress 5 s. The two forward trucks parked seated about 21.86 m apart and Unit 3 reached the bay within 4.61 m. Preflight identified a rusty Czech hedgehog at `<1458.49,36.28,3061.60>` blocking the 6.5 m route, and parked Unit 2 blocked the 8.25 m route. A 10 m south-side route passed sampled grade, road proximity, vehicle occupancy, and entity trace checks, so the test owner took the pilot seat and began the scripted pass. The lead then remained at `<1442.06,36.35,3060.08>` with `physical_path=0` throughout the two-minute pass window; `AUTO_TWO_AHEAD_RESULT: FAIL AI-piloted owner vehicle did not physically pass parked-ahead line` at 13:45:19. The owner never moved far enough for production `FORWARD_OUTBOUND_HOLD_REQUESTED`, and explicit resume was not issued. A likely test-harness cause is the smoke probe clearing POSTFRAME on the shared lead entity when releasing its arrival brake; this needs a separate callback/drivetrain retest. Log `.cache/workbench-gui-runs/2026-09-25T18-39-25-947Z-13104/console.log`; video `.cache/test-videos/auto-3truck-forward-twoahead-wide-v11.mp4`. |
| Width-8 road, final approach v12a | Same centered width-8 world; test-only owner-bypass callback fix | Settle three trucks on road before retesting a physically driven owner pass. | **V12a strict FAIL before any forward order (September 25, exit 1; zero relevant runtime errors).** Lead drove 186.599 m and en-route quality stayed bounded (peak gap 165.167 m, warning 0 s, nonprogress 4 s), but Unit 2 veered from road distance 2.68 m at 13:52:35 to 8.02 m at 13:52:39 and stopped seated with NULL waypoint and brake engaged. On this surveyed 8 m road the production and test allowance is 6 m, so the three-truck final stability gate correctly failed. `AUTO_RESULT: FAIL` at 13:55:04, `AUTO_TWO_AHEAD_RESULT: FAIL` at 13:56:13. The callback fix, physical bypass, outbound hold, and explicit resume were **not exercised**. Log `.cache/workbench-gui-runs/2026-09-25T18-49-56-105Z-33776/console.log`; finalized diagnostic video `.cache/test-videos/auto-3truck-forward-twoahead-wide-v12.mp4`. A same-pack rerun is planned to measure variability before changing the fixture or controller. |
| Width-8 road, same-pack v12b repeat | Same world and build as v12a | Distinguish route variability from the new owner pass behavior. | **V12b road arrival and two forward parks PASS; full strict gate FAIL (September 25, exit 1; zero relevant runtime errors).** The lead route covered 184.934 m and settled with final widest gap 13.334 m. Units 1 and 2 parked seated ahead about 23.84 m apart; Unit 3 reached the bay. Before driving the owner lead, the test-only safety preflight rejected all south-side bypasses: 6.5 m hit a pine near `<1456.15,36.32,3062.38>`, 8.25 m intersected parked Unit 2, and 10 m hit another pine near `<1503.89,35.16,3075.15>`. The owner never drove past the line, so outbound hold, explicit resume, and the v12 callback fix remain untested. `AUTO_TWO_AHEAD_RESULT: FAIL no clear physical owner-vehicle route past parked line` at 14:03:08. Log `.cache/workbench-gui-runs/2026-09-25T18-57-35-768Z-22912/console.log`; finalized diagnostic video `.cache/test-videos/auto-3truck-forward-twoahead-wide-v12-repeat.mp4`. A short static corridor survey is staged to choose a safer side/stop before another full run; its geometry alone cannot prove driving. |
| Owner map panel, Hold and Resume | `Worlds/Tests/ConvoyFollower_Arland_OpenRoad_PanelPilot.ent` | With the owner in the lead driver seat, open the normal map panel, inspect the server roster, issue Hold and Resume, close the map, then verify steering and throttle again. | **Latest DPI v3 F5 showed a legible, usable panel.** The supported MapMenu path opened it; mouse clicks issued Hold and Resume, and authoritative snapshots progressed to `completed: all trucks holding in vehicles` then `completed: convoy following`. **CLOSE MAP** returned to third-person vehicle view. Matching log: `.cache/workbench-gui-runs/2026-09-25T16-37-32-174Z-20256/console.log`; [48-second edited proof](media/convoy-panel-hold-resume-proof.mp4). Direct **M** input and restored throttle/steering after closing remain unverified. The earlier second F5 scaling failure is retained as a regression risk. |
| Everon terrain staging | `Worlds/Tests/ConvoyFollower_Everon_Offroad_Auto_1Truck.ent` | Probe a field route near Regina using a lead and one follower. | Scene exists and uses the generic road-goal scanner; **it is not yet an off-road PASS test**. A terrain-specific goal and measured time away from the mapped road are still required. |

## September 26 packed-client and atlas checkpoint

The frozen packaged checkpoint passed the comparable **open-road 1/2/3-truck** matrix in separate standalone clients with `--variant open-road --require-pass` (exit 0 for each). One truck logged sustained road following, peak link gap 97.66 m, no range-warning time, and 3 s nonprogress. Two trucks reached a stable on-road stop after the lead drove 322.445 m; final widest link gap 17.75 m, peak 114.56 m, warning 0 s, nonprogress 6 s. Three trucks reached a stable on-road stop after 330.998 m; final widest link gap 16.85 m, peak 127.55 m, warning 0 s, nonprogress 7 s. All three strict reports found zero relevant engine, world, resource, script, or replication errors. Logs: `.cache/client/runs/convoy-packed-openroad-{1truck,2trucks,3trucks}-1/logs/console.log`. These are autonomous route/settling checks with an AI-piloted lead, not manual driving, dirt-road, arrival return, or UI proof.

The same frozen pack also passed `--variant pause-resume --require-pass` in a separate two-truck client. The owner exited for 15 s and reboarded; both followers kept their assigned seats and completed the second leg (lead 234.506 m, Unit 1 245.556 m, Unit 2 229.159 m). The final widest gap was 13.03 m and the strict report found zero relevant runtime errors. Log: `.cache/client/runs/convoy-packed-pause-resume-2trucks-1/logs/console.log`. This is a scripted AI-piloted lead and owner-passenger sequence; manual input restoration remains untested.

The first standalone packed-client run of the width-8 three-truck world reached `GAME` and passed stable road arrival after the test lead drove 185.833 m. The server accepted two separate **Pull ahead and wait** orders. Units 1 and 2 physically parked ahead, seated, 26.716 m apart; Unit 3 reached the unload bay within 6.764 m. Both parked trucks remained stationary, and the alignment probe measured heading dots of 0.995 against the road tangent. The complete `--variant forward-twoahead-wide --require-pass` gate **failed** because every checked physical route for the player's lead around the parked line hit a tree or a truck. No owner pass or explicit resume occurred. The standalone runner's `GAME` exit status is not a convoy success marker. Evidence: `.cache/client/runs/convoy-packed-wide-3truck-1/logs/console.log`.

The first Workbench road atlas reached `GAME` and scanned 13 connected width-8 slots, reporting zero static clear bypasses. This zero is **inconclusive**: after moving proxy trucks between stations, the physics broadphase sometimes returned a truck whose current origin was roughly 48 m from the query. The survey must recheck current vehicle geometry and rerun before selecting a physical pass site. Evidence: `.cache/workbench-gui-runs/2026-09-26T00-46-16-016Z-20024/console.log` and `.cache/test-videos/pass-corridor-atlas-v1.mp4`. The atlas moves test proxies; it does not prove any convoy maneuver.

The corrected atlas v4b reached `GAME` and found three slots with statically clear corridors among the first ten of thirteen candidates. Road 81, index 9, has a 12–14 m south shoulder, a statically clear 10 m bypass, and a modeled 13.754 m lateral separation between the staged lead and Unit 1's expected final approach (the production guard is 8 m). It also has four on-road start stations 185–245 m before the bay. The atlas checks terrain, scenery, parked vehicle footprints, and a modeled approach; **it has not driven an M923 through that route**. Road 81 index 8 also has clear paths but less modeled approach clearance. Log: `.cache/workbench-gui-runs/2026-09-26T01-29-07-926Z-21952/console.log`, terminal `ROAD_ATLAS_RESULT: DONE clear_static_slots=3 scanned=10 of 13`. A separate index-9 fixture and physical pass test are the next gate.

The road 23 three-truck fixture proved stable road arrival in two F5 runs (177–178 m lead travel, final widest gap 13–16 m). The first stopped before a forward order because its test shoulder was 0.294 m outside an arbitrary 25 m staging cutoff. After widening only that test tolerance to 30 m, production correctly rejected the order: the owner's staged lead remained within 8 m laterally of Unit 1's actual approach. No forward park or return occurred. Logs: `.cache/workbench-gui-runs/2026-09-26T01-07-01-195Z-8128/console.log` and `.cache/workbench-gui-runs/2026-09-26T01-12-53-342Z-36484/console.log`; videos `.cache/test-videos/road23-3truck-forward-twoahead-v{1,2}.mp4`. This supports leaving the product lane guard intact and selecting a genuinely clearer test location.

The first road 81 full F5 run was invalidated by a duplicate test-world entity set in both its `.ent` and layer file. It logged two simultaneous lead trucks and probes; the test lead backed into the duplicate line. That scene-definition error was corrected to one layer-owned set before retesting. The corrected v2 run then passed its physical 181.262 m three-truck road approach with a final 13.54 m widest gap, zero range-warning time, 7 s maximal nonprogress, and zero relevant runtime errors. The test-only lead relocation to a south shoulder occurred **after** arrival and moved roughly 25 m, triggering `ARRIVAL_RESUMED`; Unit 1 did not regain release readiness and no forward order ran. This is a failed test sequence for arrival controls, not evidence of a physical forward park. Logs: `.cache/workbench-gui-runs/2026-09-26T01-38-32-700Z-36288/console.log` (invalid fixture) and `.cache/workbench-gui-runs/2026-09-26T01-44-48-167Z-39540/console.log` (valid approach, failed staging). Finalized diagnostics: `.cache/test-videos/road81-3truck-forward-twoahead-v{1,2}.mp4`. The next fixture stages a checked bay-side shoulder when the lead first reaches its road goal, before the followers settle.

The corrected road 81 v3 run staged the lead on the south shoulder **before** follower arrival. Its lead covered 192.967 m and stopped 11.90 m from the mapped road. Unit 1 settled 19.69 m from the lead and 5.66 m from the road; Unit 2 followed it to 13.15 m off the road, then logged `ARRIVAL_ROAD_BLOCKED: ... no reachable predecessor road point`. The three-truck arrival never stayed stable, and the strict `--variant forward-twoahead-road81 --require-pass` gate exited 1 with `AUTO_RESULT: FAIL`; no forward order, player pass, or resume occurred. The finalized diagnostic video shows the trucks scattered around the bay rather than queued cleanly. This is evidence that a large lateral lead relocation and the current final-approach logic do not yield a usable unloading stop. The next comparison narrows only the **test lead's** shoulder offset; it keeps the production lane guard and release rules unchanged. Log: `.cache/workbench-gui-runs/2026-09-26T01-54-17-222Z-39720/console.log`; video: `.cache/test-videos/road81-3truck-forward-twoahead-v3.mp4`.

The frozen standalone road 81 v4 run used an 8.87 m shoulder offset. It logged a stable three-truck arrival after 190.678 m lead travel (widest link 14.00 m), then Unit 1 drove to its forward slot and remained seated, braked, and stationary. Unit 2 advanced to within 2.31 m of the saved unload bay. Its next release was rejected with `Move your lead vehicle off the driving lane to let this truck pass`, so the full sequence **failed** before a second park, owner pass, or resume. The strict parser also found support-station and resource-leak diagnostics emitted during client shutdown; these remain recorded errors. The next code comparison measures the lead against the actual forward road corridor instead of a successor's potentially diagonal recent approach, retaining the same 8 m clearance margin. Geometry and a live blocked-lane control must pass before accepting that change. Log: `.cache/client/runs/road81-v4c-3truck/logs/console.log`; video: `.cache/test-videos/road81-3truck-forward-twoahead-v4-standalone.mp4`.

## Workbench runs

Open one world at a time, with trusted local test-script authorization, then press F5. Workbench logs append multiple F5 and editor reload sections, so use the saved `console.log` from the current GUI run with `--require-pass`.

```powershell
npm run workbench:open -- --editor world --project addons/ConvoyFollower/addon.gproj --world Worlds/Tests/ConvoyFollower_Arland_Auto_2Trucks.ent --authorize-local-test-scripts --execute
npm run convoy:test -- --trucks 2 --variant harbor --report .cache/workbench-gui-runs/<run>/console.log --require-pass

npm run workbench:open -- --editor world --project addons/ConvoyFollower/addon.gproj --world Worlds/Tests/ConvoyFollower_Arland_OpenRoad_Auto_2Trucks_PauseResume.ent --authorize-local-test-scripts --execute
npm run convoy:test -- --trucks 2 --variant pause-resume --report .cache/workbench-gui-runs/<run>/console.log --require-pass

npm run workbench:open -- --editor world --project addons/ConvoyFollower/addon.gproj --world Worlds/Tests/ConvoyFollower_Arland_OpenRoad_Auto_2Trucks_ForwardWait.ent --authorize-local-test-scripts --execute
npm run convoy:test -- --trucks 2 --variant forward-wait --report .cache/workbench-gui-runs/<run>/console.log --require-pass

npm run workbench:open -- --editor world --project addons/ConvoyFollower/addon.gproj --world Worlds/Tests/ConvoyFollower_Arland_OpenRoad_Auto_2Trucks_ForwardBlocked.ent --authorize-local-test-scripts --execute
npm run convoy:test -- --trucks 2 --variant forward-blocked --report .cache/workbench-gui-runs/<run>/console.log --require-pass
npm run convoy:test -- --trucks 3 --variant forward-twoahead --report .cache/workbench-gui-runs/<run>/console.log --require-pass

npm run workbench:open -- --editor world --project addons/ConvoyFollower/addon.gproj --world Worlds/Tests/ConvoyFollower_Arland_WideRoad_Auto_3Trucks_ForwardTwoAhead.ent --authorize-local-test-scripts --execute
npm run convoy:test -- --trucks 3 --variant forward-twoahead-wide --report .cache/workbench-gui-runs/<run>/console.log --require-pass
```

The pause-resume parser requires the exact pause-resume world, `AUTO_INIT: expected=2`, GAME, passing en-route metrics, a passing road arrival, and `AUTO_PAUSE_RESULT: PASS`. A successful dismount request alone cannot pass. Inspect the video for clipping, unsafe pedestrian exits, and how the trucks behave while the owner is on foot.

All strict variants reject ENGINE, WORLD, RESOURCES, SCRIPT, or RPL errors in the selected run. The parser counts seven exact repeatable pre-init base-world diagnostics separately. It also recognizes the exact vanilla helipad prefab warning only when the matching base-prefab load immediately precedes it and no convoy gameplay action has begun; that warning can appear after GAME in a vanilla load. A changed warning, missing prefab context, or error during gameplay fails the gate.

The [67-second pause/resume proof clip](media/convoy-pause-resume-proof.mp4) is cropped from the September 25 passing Workbench recording. Its five brief captions identify log-verified stop, owner exit, reboard, moving chain, and final strict PASS; the wide camera does not make the player avatar easy to see, so the exit/reboard claims come from the authoritative probe, not a close-up. Source: `.cache/test-videos/auto-2truck-pause-resume-v2.mp4`, corresponding `console.log` under `.cache/workbench-gui-runs/2026-09-25T15-03-50-670Z-25820/`. The 1280×720 clip has no audio because the source desktop recording had no audio stream.

The separate forward-wait world uses `--trucks 2 --variant forward-wait`. Its parser requires `AUTO_FORWARD_RESULT: PASS` in addition to the normal route evidence. The companion issues `CF_PanelPullAhead` to the stable Unit 1 identity and checks actual seated forward parking, at least 15 m forward movement and bay clearance, and Unit 2 reaching the bay. A clicked or accepted order alone is insufficient.

The [37-second forward-wait proof clip](media/convoy-forward-wait-proof.mp4) is a continuous, cropped portion of the passing V3 Workbench recording **before** the current 8 m width guard. It shows the lead on the shoulder, Unit 1 pulling out toward the right edge of the fixed camera, and Unit 2 moving into the bay. Unit 1's final forward waiting slot lies outside that camera view, so the parked/seat claim comes from `FORWARD_WAIT_PARKED` and the strict physical-position probe, not from a visual close-up. It does not demonstrate player map input, a second forward release, or rejoining after passing. Source: `.cache/test-videos/auto-2truck-forward-wait-v3.mp4`, with matching log `.cache/workbench-gui-runs/2026-09-25T15-34-33-494Z-33964/console.log`. The later road-width survey measured this road at 3.5 m; the new wide-road fixture is the intended current-build positive run.

The [40-second blocked-lane proof clip](media/convoy-forward-blocked-proof.mp4) is a continuous crop from `.cache/test-videos/auto-2truck-forward-blocked-v1.mp4`. It shows the lead occupying the travel lane and the first follower remaining at the stop. Its caption states the log/probe result; the video does not itself reveal the server's rejection reason or prove seat occupancy. The matching strict log above records the exact blocked-lane reason, an occupied driver seat, and only 0.054 m movement over the five-second hold. This is evidence of refusal under obstruction, not successful forward parking.

The [48-second panel proof clip](media/convoy-panel-hold-resume-proof.mp4) joins three excerpts from the DPI v3 Workbench recording: panel open, Hold, and Resume plus Close. The map panel text is legible at the captured scale. The authoritative log above records accepted commands followed by the two completed states; the displayed transitions match it. The clip shows the third-person vehicle view after Close, but it does **not** establish that throttle and steering inputs resumed or that pressing M directly opens the map. Source: `.cache/test-videos/panel-pilot-dpi-v3.mp4`.

The [60-second V8 partial two-ahead clip](media/convoy-twoahead-parking-partial-v8.mp4) crops the same fixed road view from the run logged at `.cache/workbench-gui-runs/2026-09-25T18-08-36-919Z-6592/console.log`. It shows the two trucks taking separate forward road positions while the third approaches through partly obscuring trees. The exact 26.35 m parking separation, seat occupancy, and Unit 3 bay arrival are log/probe findings. The clip deliberately says **partial proof**: the owner lead failed to pass the parked line afterward, and the three-truck resume gate failed. It is not a release-ready convoy-return demonstration.

The forward test must park the owner's lead vehicle off the travel lane before issuing the order. The current straight-road route stops the AI lead near the lane center, which can make passing physically impossible. The fixture moves **only the test lead** 10–12 m laterally to a level, entity-clear shoulder after the normal road-arrival gate, logging both positions as `AUTO_FORWARD_FIXTURE_SHOULDER`. It also checks for a connected road point at least 75 m ahead. After relocating the lead, it waits for the server's actual `CanReleaseAtUnload` gate and logs `AUTO_FORWARD_WAIT_READY`; a 25-second timeout fails instead of pretending an order was issued. This scripted lead staging does not count toward the PASS gate; the release truck itself must drive, clear the bay, park on a connected road slot, and let Unit 2 advance under the real convoy code. A blocked-lane preflight should report unable to proceed, not a pass.

The harbor-gate run should be compared against the same build on the open road. If only the harbor scene fails, capture each unit's position, current waypoint, target/predecessor, road-center distance, throttle, brake, gear, and seconds without progress around the gate; the existing probe logs these via `AUTO_UNIT_NAV`. This identifies a turn/navigation failure separately from a general inability to follow. Keep a 1/2/3 count matrix and do not average away a failing count.

The Everon off-road scene currently gives useful staging and launch coverage only. Do not report it as successful off-road pathing based on an AI road waypoint, a package build, or a short movement clip. The acceptance gate should require an explicitly surveyed terrain target, a significant period with both vehicles beyond the mapped road edge, no prolonged immobility, and visible footage confirming the surface is drivable. Add those conditions after a terrain survey; this prevents a road-hugging path from falsely passing an off-road test.
